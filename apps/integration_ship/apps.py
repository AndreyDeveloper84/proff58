from django.apps import AppConfig


class IntegrationShipConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.integration_ship"
    verbose_name = "Интеграция перевозчиков"
