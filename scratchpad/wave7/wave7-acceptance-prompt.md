# Стартовый промпт окна Wave 7.1 — ACCEPTANCE

Подготовлен оркестратором 2026-07-27 после приёмки H5. Опора: §7 плана волны.
Все пять стадий H1–H5 приняты и в `origin/dev = 28c7bef`, CI зелёный, деплой прошёл.

---

Окно: Wave 7.1 — acceptance волны (§7 плана). Ветка dev. Общение только на русском.
Это НЕ стадия разработки: новых возможностей контура не добавляем.

СТАРТОВОЕ ЧТЕНИЕ — ровно четыре вещи:
1. docs/plans/2026-07-26-WAVE7_1_H3_H5_PLAN.md — §7 (acceptance), §1 (зачем волна,
   findings A/B), §3 (non-scope), §8 (что идёт после);
2. scratchpad/wave7/wave7-h4-orchestrator-report.md и wave7-h5-orchestrator-report.md
   (самодостаточные, читать целиком);
3. scratchpad/wave7/wave7-h1-report.md, h2, h3 — только grep по разделам «итог» и
   «проверки», целиком не тянуть;
4. CLAUDE.md §7 — там формулировки инвариантов, которые придётся править.

СОСТОЯНИЕ НА ВХОДЕ
  origin/dev = 28c7bef, все H1–H5 запушены, локальная ветка синхронна
  taxonomy   identity=fc13be78…  semantic=d906be2f…  options=328  pending=0
  release    canonical_hash=e0ff608e…  --check = ok
  gate       rows=103 correct=102 precision=0.9902912621359223, CI без поблажки
  regression baseline: 2 failed (окружение), 1920 passed, 1 skipped
  стенд      задеплоен, reconcile blocking=0, pending_business_review=0

ЗАДАЧИ (§7 плана)

1. СВОДНЫЙ ОТЧЁТ ВОЛНЫ: findings A/B закрыты, чем именно доказано.
   A — неканоническая идентичность словаря типов.
   B — gate верил самодекларированным полям артефакта (фальшивый sample давал
   precision=1.0).
   По каждому: что было, что сделано, каким проверяемым фактом доказано (хэш, дифф,
   номер CI-прогона, guard-тест). Не пересказ стадий — доказательная сводка.

2. НЕЗАВИСИМАЯ ПРОВЕРКА ВТОРЫМ МНЕНИЕМ — /codex review по диффу волны.
   ВНИМАНИЕ на границы диффа: волна = 21 коммит с меткой «Wave 7.1», первый — e3e0797
   (H1.1), вершина — 28c7bef. НО сплошной диапазон e3e0797^..28c7bef захватывает чужие
   треки (правила perforatory из PR #593/#594 — это трек 2, не волна). Ревью гнать
   либо по списку коммитов волны, либо по путям контура:
     apps/catalog/{taxonomy_manifest,rules_gate,rules_release,rules_engine,
                   tool_type_rollback,taxonomy_reverse}.py
     apps/catalog/management/commands/catalog_{rules_*,taxonomy_*,tool_type_*}.py
     apps/catalog/tests/ (файлы контура и H5)
     data/catalog_processing_rules/
     .github/workflows/tests.yml
     docs/catalog/{rules-gate-h2,rules-release-manifest,tool-type-*}.md
   Находки codex РАЗОБРАТЬ поимённо: принято / отклонено с обоснованием. Приложить
   сырой вывод недостаточно.

3. ПРОВЕРКИ: полный regression (baseline 2 failed / 1920 passed / 1 skipped — любое
   третье падение регрессия; арифметику показать), manage.py check,
   makemigrations --check --dry-run, ruff/black clean.
   Regression гонять БЕЗ параллельных прогонов против общей тестовой БД: иначе
   DeadlockDetected читается как регрессия (см. память проекта, случай H5).

4. PR в dev, зелёный CI, ревью.
   УТОЧНЕНИЕ, которого нет в плане: пункт писался в расчёте, что волна идёт веткой.
   Фактически H1–H5 уже в origin/dev. Поэтому acceptance-PR содержит ТОЛЬКО
   документарные изменения — сводный отчёт, статусы в плане, снятие заморозки.
   Кода контура в нём быть не должно. Если по ходу выяснится, что нужен код —
   это не acceptance, а новая стадия: остановиться и доложить.

5. СНЯТИЕ ЗАМОРОЗКИ Phase 8 — явной записью в плане волны и в CLAUDE.md §7
   (сейчас там: «Phase 8 (pilot rollout) заморожена до WAVE 7.1 ACCEPTED»).

ОБЯЗАТЕЛЬНЫЙ РАЗДЕЛ СВОДНОГО ОТЧЁТА — «что осталось незакрытым»
Acceptance, в котором всё идеально, — недостоверный acceptance. Перечислить минимум:
- форвардный reslug остаётся дырой: load_tool_types создаст новую опцию и оставит
  старую (no-delete), товары останутся на старой. Закрывается потребляемыми
  legacy_aliases — решение владельца, реализация ПОСЛЕ ACCEPTED;
- смена value алиасами не закрывается — там нужен option_uid (долг с триггером);
- release-evidence живёт в apps/catalog/tests/fixtures/, перенос в data/ унаследован
  из H3 как открытый вопрос;
- 4 опции с 0 товаров (manifest_unused_option=4) оставлены осознанно — не дрейф;
- --allow-legacy-taxonomy-hash сохранён для replay исторических артефактов 7B/7C/7D,
  выведен из CI и защищён guard-тестом;
- manifest_version = 1: процедура понижения N→N-1 протестирована, но реального
  перехода версии в проде ещё не было.
НЕ смешивать с треком 2: PLAN_CREATE=40 по другим tool_type — это парсер-трек,
к волне отношения не имеет, в отчёт волны не тащить.

ГРАНИЦЫ
- Новых возможностей контура не добавлять; матчер, ruleset v2, applied corpus,
  манифест и артефакты гейта не трогать. Если acceptance требует правки контура —
  это находка, а не задача окна.
- Phase 8 НЕ НАЧИНАТЬ. Снятие заморозки — это запись в документах, а не запуск
  ступеней. Первая ступень (synthetic batch, 5 фиктивных cases) — отдельное окно
  после ACCEPTED.
- Push и PR — только по явной просьбе владельца.
- Объявление «WAVE 7.1 ACCEPTED» — прерогатива владельца. Окно готовит всё
  основание и выносит на решение, само не объявляет.

ПРИЁМКА ОКНА
- сводный отчёт с доказательствами по A/B и честным разделом незакрытого;
- находки codex разобраны поимённо;
- четыре проверки из п.3 с показанной арифметикой;
- acceptance-PR только с документами, CI зелёный;
- правки статуса Phase 8 подготовлены, но ACCEPTED объявляет владелец;
- протокол: scratchpad/wave7/wave7-acceptance-report.md.

---

## Что оркестратор будет проверять при приёмке

1. **Дифф acceptance-PR не содержит кода контура** — только документы. Иначе это
   скрытая стадия под видом приёмки.
2. **Findings A/B доказаны фактами**, а не пересказом: хэш, дифф, номер CI-прогона.
3. **Раздел незакрытого не пустой** и совпадает со списком выше.
4. **Codex-находки разобраны**, а не приложены сырым выводом.
5. **Арифметика regression** от baseline 1920.
