# TT-06 · Apply одобренных findings ступени 3 — первое реальное изменение каталога контуром

Заведено оркестратором 2026-07-28 по решению владельца. Вход: ступень 3
(`scratchpad/phase8/phase8-step3-report.md`), run `00638eaa-0d7e-4532-b13f-ab40b3b8be0d`.

---

# TT-06 · Phase 8 · применение одобренных findings
# Проект «Профессионал», ветка dev. Общение только на русском.
# WRITE по каталогу. Сначала локально, затем staging — оба раза по гейт-циклу.

## КОНТЕКСТ

Ступень 3 принята. Проверено оркестратором независимым запросом к БД:

- run `00638eaa…`, статус `running`, items: 11 `processing` / 9 `needs_review`;
- `CatalogChange`: **11 approved / 1 rejected**;
- **`tool_type` у всех 11 товаров пуст** — apply не выполнялся, каталог не тронут.

Качество: 12 предложений из 20 — 10 верных, 1 условно верное, 1 ошибочное (пойманное
модерацией: катод плазмотрона предложен как «сопла», отклонён). Evidence сверены руками
по всем 20; для товара 422 недоступный источник заменён на официального дилера **до**
записи в result. Ни одного catch-all — контраст со ступенью 2.

**Это первое реальное изменение каталога контуром распознавания.** Ради него строилась
вся волна 7.1, включая обратимость (H5) и защиту от гонок (H6).

## ЗАДАЧА

Применить 11 одобренных findings: товары **4, 22, 123, 164, 179, 377, 422, 4944, 4945,
11232, 23606**. Меняется только `tool_type` (slug PAV), больше ничего.

Порядок: **локально → staging**. Локальная БД — свежая копия staging (обновлена
2026-07-28), поэтому локальный прогон даёт честное предсказание чисел, которое надо
сверить на стенде.

## ГЕЙТ-ЦИКЛ (оба раза: локально и на staging)

1. **Read-only** — отпечаток неприкасаемых полей ДО (`code_1c`, `article`, `name`,
   `category_id`, `price`, `stock_quantity`, `status`, `is_active`); текущее значение
   `tool_type` у каждого из 11 (ожидание — пусто).
2. **Preflight** — нет `content_locked`; нет значений `manual` в `tool_type` у этих
   товаров; все предложенные типы существуют в словаре (329 опций).
3. **Снимок для отката** — обязателен: контур H5 работает **по паре снимков**
   (`--from` ожидаемое текущее, `--to` цель). Снять снимок «до» командой
   `catalog_tool_type_snapshot`, зафиксировать путь.
4. **Dry-run** — план с точными числами: 11 записей, каких именно типов.
   **Ожидание сформулировать ДО прогона и сверить после.**
5. **pg_dump** — после dry-run, до записи.
6. **Apply** — одной транзакцией, с обязательным `--run <run_id>` (G1 закрыт
   процедурно).
7. **Post-audit** — `attrs_cache` пересобрать **точечно** по 11 товарам (глобальный
   `rebuild_attrs_cache` запрещён); хэш неприкасаемых полей идентичен до и после
   (меняется только `tool_type`, он в отпечаток не входит — проверить явно);
   `tool_type` проставлен ровно у 11 и ровно тот, что одобрен; дублей PAV нет.
8. **Проверить откат** — на локальной БД, до перехода на staging: выполнить
   `catalog_tool_type_rollback` по паре снимков, убедиться, что состояние вернулось,
   затем применить снова. **Это первое реальное испытание контура отката** — его нельзя
   пропускать, иначе на staging мы пойдём с непроверенной страховкой.
9. **Витрина** — 11 товаров получат тип; проверить живыми запросами, что они появляются
   в фильтре по своему `tool_type` и счётчики сходятся с БД.
10. **`finalize`** — после apply run должен закрываться штатно (`items_not_final`
    снимается). Выполнить и показать результат.

## ГРАНИЦЫ

- Применяются **только 11 одобренных**; `rejected` (товар 6798, катод плазмотрона) и
  9 `needs_review` **не трогать**.
- Ничего, кроме `tool_type`, не менять: цена, остаток, категория, название, артикул,
  статус публикации — вне досягаемости.
- Новые типы в манифест не добавлять — кандидаты (катоды/электроды плазмотронов,
  ручные гайковёрты, бойки/ударники) идут отдельной задачей пакетом.
- Контур `tool_type` не трогать: matcher, ruleset v2, applied corpus, манифест,
  артефакты гейта.
- Глобальные команды (`enrich_attributes` без `--path`, `rebuild_attrs_cache`) запрещены.
- Ступень 4 (batch 50) не начинать.
- Push и PR — только по явной просьбе владельца.
- **Перед любой работой с GitHub** — сначала `git fetch gitlab --prune` и сверка
  `origin/dev` с `gitlab/dev`.
- Рабочая копия общая: параллельно идут CAT-03 (правит `apps/catalog/{admin,facets,
  models,queries}.py` — фича `display_name` фасетов) и PARS-03. Чужие изменения **не
  откатывать**, `git add` только точечными путями.

## ЕСЛИ ЧТО-ТО ПОЙДЁТ НЕ ТАК

- расхождение предсказанных и фактических чисел → **остановиться**, не подгонять;
- откат на локальной БД не вернул состояние → **остановиться**, на staging не идти;
- любой товар получил не тот тип, что одобрен → откатить и доложить.

Откат — не аварийная мера, а штатная часть этой задачи: он проверяется намеренно.

## ПРИЁМКА

- 11 товаров получили одобренные типы, ни одного лишнего изменения;
- предсказанные числа сошлись с фактическими — локально и на staging;
- **откат испытан на локальной БД и вернул состояние** — доказано снимками;
- хэш неприкасаемых полей идентичен до и после;
- `rejected` и `needs_review` не затронуты;
- витрина проверена живыми запросами;
- run закрыт `finalize`, состояние показано;
- pg_dump и снимки отката сохранены, пути указаны;
- протокол `scratchpad/phase8/tt-06-report.md` + самодостаточный отчёт для оркестратора.

---

## Что оркестратор будет проверять при приёмке

1. **Ровно 11 товаров** изменены, `rejected` и `needs_review` нетронуты.
2. **Откат реально испытан** на локальной БД — это главная ценность задачи помимо
   самого apply.
3. **Отпечаток неприкасаемых полей** идентичен: цена, остаток, категория, статус целы.
4. **Числа предсказаны до прогона** и сошлись — на обеих БД.
5. **`--run` в команде apply**, глобальные команды не запускались.
