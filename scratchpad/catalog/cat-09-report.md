# CAT-09 · Протокол: перенос кластеров из `prochaya-osnastka` в готовые типы (класс 2, recat)

**Дата:** 2026-07-29 · **Стенд:** `proff58_staging-web-1` (dev.proff58.ru) · **Ветка:** `dev`
**Промпт:** `scratchpad/catalog/cat-09-osnastka-recat-prompt.md`
**Маршрут:** плейбук `docs/catalog/operations/recategorize.md`, цикл TT-08
(снимок → pg_dump → одна `transaction.atomic` → `bulk_update` PAV `value_option`
+ точечный `attrs_cache` → post-audit). Менялся **только `tool_type`**.
`source` записей не трогался (решение владельца в TT-08: `manual` остаётся).

**Кто ещё работал в окне времени (по требованию промпта):**
- **CAT-08** (чистка префикса `яя` в `Product.name`): его staging-запись
  (4545/4550 названий, только поле `name`) приземлилась между моим prep и
  предзаписной сверкой S1 (~15:40–15:50 по серверу). Обнаружена сменой
  отпечатка `560f7ee7…` → `92587abc…` и построчным диффом проекций. Запись
  S1 **остановлена до подтверждения владельца**; CAT-08 сдал протокол в 15:34
  (5 мусорных исключений, больше записей не будет), после чего S1 проведён.
  Урок дня учтён: сверка делалась по стенду, не по локальному состоянию.
  Побочное наблюдение от владельца: у 9 имён вида `яяЯкорь`/`яяЯщик` первый
  проход снял 3 буквы, CAT-08 исправил в той же сессии — в моём диффе по ним
  движение именно это.
- **TT-10** (9 точечных recat) на момент всех четырёх write **не писал**:
  предзаписная сверка S2 показала все 9 его id на исходных типах.
  Пересечений с кластерами CAT-09 нет.

---

## Итог одной строкой

**269 товаров** перенесено в 4 существующих типа за 4 кластера, каждый — со
своим GO владельца. `prochaya-osnastka`: **1849 → 1580** (видимых 519 → 447).
PAV total 38833 неизменен. Отпечаток неприкасаемых полей идентичен во всех
четырёх циклах (`92587abc…` до и после каждого).

| Кластер | moved | Целевой тип | Тип до → после (всего/вид.) | prochaya после (всего/вид.) |
|---|---:|---|---|---|
| S1 смазки | 88 (32 вид.) | `str-smazki` | 41/14 → **129/46** | 1761/487 |
| S2 лебёдки/тали | 82 (16 вид.) | `lebedki-tali` | 73/24 → **155/40** | 1679/471 |
| S3 ящики/контейнеры | 59 (15 вид.) | `yashchiki-sumki` | 287/161 → **346/176** | 1620/456 |
| S4 тележки | 40 (9 вид.) | `obor-telezhki` | 15/5 → **55/14** | **1580/447** |

Все восемь предсказанных пар чисел сошлись с фактом. Дублей PAV нет,
`attrs_cache ≡ EAV` по всем 269 (postcheck в драйвере).

---

## S1 · смазки → `str-smazki` (88)

- **Критерий воспроизведён:** `tool_type=prochaya-osnastka AND name ILIKE
  '%смазка%'` → **88 (32 вид.)** — точно как в разведке CAT-07.
- **Все 88 названий просмотрены глазами.** Ложных нет: все — сама смазка
  (88/88 первым словом «Смазка», incl. один бывший `яя`). Шприцов/маслёнок/
  насадок «для смазки» в выборке не оказалось. FP=0.
- **Расхождение «33 видимых локально vs 32 на стенде»** (вопрос владельца) —
  подтверждено именно яя-деактивацией CAT-07: 33-я — `28259` «Смазка
  универсальная Hitachi 30 гр.», на стенде `is_active=False,
  status=published, stock=0, is_active_1c=True` (паттерн деактивации).
  Не округление.
- **Откат испытан** (первый кластер, по промпту): dry-run `write=88
  conflict=0` → `--apply` `written=88, post-audit=PASS` → состояние вернулось
  (41/14, 1849/519) → forward повторно (`moved=88`, postcheck ok) →
  финальные счётчики 129/46, 1761/487.
- Витрина: `tool_type=str-smazki` глобально **46** == БД; в категории
  `osnastka-prochaya` **32** == БД; `prochaya-osnastka` **487** == БД.

## S2 · лебёдки/тали/тельферы → `lebedki-tali` (82)

- **Расхождение критерия разобрано до записи:** формулировка разведки
  «`^таль|^тельфер|^лебедк` + категория [169]» читалась как AND → 65.
  Фактический критерий — **по имени независимо от категории**: 82 (16 вид.),
  точно как в разведке; категория [169] целиком входит в выборку (65 ⊂ 82),
  остальные 17 — тали/тельферы в категориях 166/217/405. Число не подгонялось.
- **Все 82 названия просмотрены глазами** — все тали/тельферы. Единственная
  оговорка: `27828` «Таль балансир» — пружинный балансир инструмента, но
  грузоподъёмное устройство в категории 169, ближайшего типа нет; оставлен
  в кластере, зафиксировано здесь. FP=0.
- **Пересечение с TT-10 учтено:** TT-10 заберёт ИЗ `lebedki-tali` четыре
  товара (34643, 34644 → `sterzhni-kleevye`; 35057, 35058 → `passatizhi`).
  После S2 тип = **155/40**; после TT-10 станет **151/36**. Предзаписная
  сверка: все 9 id TT-10 на исходных типах, пересечений с кластером S2 нет.
- Витрина: `tool_type=lebedki-tali` глобально **40** == БД;
  `prochaya-osnastka` **471** == БД.
- Наблюдение (не scope CAT-09): в `lebedki-tali` уже лежал мусор
  (напр. `32189` «Кабелерез») — это контур TT-09/TT-10, CAT-09 не трогал.

## S3 · ящики/контейнеры → `yashchiki-sumki` (59 из 81, сплит — решение владельца)

- Разведка дала 81 (18 вид.) по «категория [214] + `^контейнер`». **Все 81
  просмотрены глазами.** Найден 21 сомнительный + 1 пищевой; вынесено
  владельцу, принят **вариант «59»**:
  - **перенесено 59:** 45 ящиков [214] + 14 контейнеров хранения;
  - **исключено 22:** 13 мусорных/медицинских/«для сбора нефтепродуктов»
    контейнеров (типа в манифесте нет — проверено по
    `tool_type.v2.json` — стоп-правило плейбука), `26654` крышка и `27278`
    ремень (аксессуары, типа нет), `27909/27910` тележки для ящиков
    (ушли в `obor-telezhki` в S4), баки 120л `26612/26613`;
  - **критерий исключения дословно (владелец):** «пищевая тара — не
    инструментальный ящик, независимо от объёма и категории» — по нему
    дополнительно исключены `28109` «Ящик п/э сырково-творожный» и
    `26582` «Контейнер Кубик 500мл».
- Контроль целевого типа: в `yashchiki-sumki` (287) — инструментальные
  ящики/органайзеры/сумки, мусорных контейнеров 0 → сплит не размывает тип.
- Видимые пересчитаны окном: в списке 59 опубликованных 15 (`28109` не
  опубликован). Предсказание 287→346 (161→176), 1679→1620 (471→456) —
  сошлось.
- Витрина: `tool_type=yashchiki-sumki` глобально **176** == БД;
  `prochaya-osnastka` **456** == БД.

## S4 · тележки → `obor-telezhki` (40 из 42, решение владельца)

- **Вопрос `obor-telezhki` vs `hoz-tachki` вынесен владельцу с числами и
  содержимым обоих типов** (obor 15/5 — инструментальные тележки, бочкокаты,
  ручные грузовые; hoz 55/30 — садовые/строительные тачки). **Решение
  владельца: `obor-telezhki`** — складская грузовая тележка это оборудование;
  в `hoz-tachki` кластер размыл бы тип.
- **Кандидатов 42, а не 41 из разведки — находка `28268`:** «Тележка
  паллетная RHP 2500», бывший `яя`-товар (`original_name` с префиксом);
  CAT-08 снял префикс, и товар **вернулся в зону видимости правил** —
  подтверждение, что чистка CAT-08 работала не только на витрину. Товар не
  опубликован, pub-числа не изменил.
- Глазной разбор: 40 складских/платформенных/паллетных тележек — чисто.
  По решению владельца **исключены `27906/27907` «Тележка для тали»**
  (каретка по балке — не везёт груз сама; типа «аксессуары для талей» нет —
  стоп-правило, остаются в `prochaya-osnastka`). **`27940` «Тележка
  покупательская 100л» включена** (владелец: «механически и по сценарию это
  тележка»). Также в кластере `27905` (тележка для колёс грузовых авто) и
  `27947` (чан для фарша — в типе уже есть аналог «Тележка 200л ФЦ1В…для
  мясосырья»).
- Предсказание 15→55 (5→14), 1620→1580 (456→447) — сошлось.
- Витрина: `tool_type=obor-telezhki` глобально **14** == БД;
  `prochaya-osnastka` **447** == БД.

---

## Инварианты (все четыре цикла)

- **Отпечаток неприкасаемых полей** (`code_1c, article, name, category_id,
  price, stock_quantity, status, is_active`; 47 225 строк, SHA-256 проекции)
  — `92587abc5863…` **идентичен** до и после каждого write. Перед S1
  переснят после завершения CAT-08 (см. шапку).
- **PAV total 38833** неизменен — значение переставлялось, не добавлялось.
- Дублей PAV нет; `attrs_cache ≡ EAV` по всем затронутым (postcheck).
- Категория, цена, остаток, публикация, `source` — не трогались.
- Новые типы не заводились; контур `tool_type` (манифест, gate, ruleset)
  не тронут; `provenance` не правился и не обходился; глобальные команды
  не запускались; push/PR не выполнялись.

## Остаток и следующие шаги (не начаты — нужны отдельные GO)

- **Доводчики (42/4) → `hoz-furnitura` и втулки (21/12) → `zap-vtulki`** —
  по промпту не начинать без отдельного слова владельца: оба типа общие,
  попадание надо обсудить.
- Остаток `prochaya-osnastka` после CAT-09: **1580 всего / 447 видимых**.
  Крупнейшие оставшиеся кластеры (карта CAT-07): стропы (208, типа нет),
  съёмники (137, типа нет), патроны (109, типа нет), головки триммерные
  (93, типа нет) — это TT-контур заведения типов, не CAT-09.
- Мусор внутри существующих типов (`32189` кабелерез в `lebedki-tali` и т.п.) —
  контур TT-09/TT-10.

## Артефакты

`scratchpad/catalog/cat-09/`:
- разведка: `cat09_recon.py/json`, `cat09_recon2.py/json`, `cat09_s3_recon.py/json`;
- rollback-map: `cat09-s{1,2,3,4}-rollback.json` (88/82/59/40 строк);
- снимки H5: `cat09-S{1,2,3,4}-{before,after}.json`;
- драйверы: `cat09_s{1,2,3,4}_{prep,batch,audit}.py`, `cat09_s{1,2,3,4}_run.sh`,
  `cat09_s1_rollback_test.sh`, `cat09_fingerprint.py`,
  `cat09_s{1,2}_prewrite_check.py`;
- витрина (живые ответы): `live_*.json`;
- дампы на стенде: `/home/taximeter/backups/staging/db-cat09-S{1,2,3,4}.sql.gz`
  (~21,5 МБ каждый, db-only, перед каждым write).

**Записей в локальную БД не было — вся работа на staging.**
