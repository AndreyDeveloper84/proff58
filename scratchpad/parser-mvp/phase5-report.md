# Протокол работы — Трек 2 · Phase 5 (PARS-05): полный прогон по гейт-циклу

Дата: 2026-07-28, ветка `dev`. Прогон выполнен **локально**; staging — не
трогал до GO владельца; staging-прогон — §7a. Phase 6/7 не начинал, приёмку MVP не
объявляю.

## 1. Полный сбор (режим A, прежний режим доступа)

```
resanta:   9 карточек   vihr: 12   interskol: 20   zubr: 20   ИТОГО: 61, отклонено 0
```

Журнал `phase5-fetch-log.jsonl` (69 строк): все статусы **200**, отказов
401/403/429 — 0, `cache_hit` 36 (страницы Phase 2 + листинги). Темп: пауза
3.0 с между СТАРТАМИ запросов к хосту по построению `PoliteClient._throttle`
(`DEFAULT_THROTTLE_S = 3.0`, метка `time.monotonic()` после сна; журнальный
`throttle_wait_s` — лишь остаток паузы за вычетом времени обработки ответа,
поэтому 0.7–1.9 с — это не нарушение, а артефакт измерения, как в Phase 2 §2).
User-Agent честный, с контактом. Выгрузки — `phase5-output/`, схема
`Export`/`ErrorsExport` валидна, errors-файлы пустые.

## 2. Замер до записи (`--dry-run`, отчёт `phase5-dryrun.json`)

| Исход | Значений |
|---|---|
| confirm (совпало с существующими — без изменений) | 93 |
| **overwrite** | **0** |
| create (в пустые поля) | 13 |
| skipped_priority | 0 |
| skipped_voltage (правило «только аккумуляторным») | 20 |
| dropped | 26 |

**Поимённый перечень перезаписей — ПУСТОЙ.** Все сравнимые значения совпали с
проверенными regex (доля расхождений 0/93 = **0 %**, порог 10 % не затронут).
Единственный прецедент расхождения (44544, 5,5 → 5,2 Дж) был показан и записан
в PARS-04 и теперь проходит как confirm.

**create поимённо (13):**

| Товар | Атрибут | Значение | Поле источника |
|---|---|---|---|
| 44507 ИНТЕРСКОЛ П-32/1100АВС | power | 1100 | Мощность, Вт. |
| 44507 | energy_impact | 3.4 | Max сила удара, Дж |
| 44507 | no_load_speed | 1200 | Частота вращения на холостом ходу |
| 44507 | chuck | sds-plus | Тип хвостовика |
| 44507 | motor_type | brushed | Тип двигателя |
| 44507 | power_source | mains | derived (контекст категории) |
| 44522 ЗУБР ЗП-2470 | no_load_speed | 1100 | Частота вращения шпинделя |
| 44524 ЗУБР ЗП-2680 | no_load_speed | 900 | Частота вращения шпинделя |
| 44538 ЗУБР П-22-650 | no_load_speed, energy_impact | 870, 2.3 | Частота вращения / Макс. энергия |
| 44539 ЗУБР П-24-750 | no_load_speed, energy_impact | 870, 2.4 | — // — |
| 44540 ЗУБР П-26-800 | no_load_speed | 1300 | Частота вращения шпинделя |

**Матчинг:** matched 21, ambiguous 6, not_found 34.
- ambiguous (в отчёт, не в базу): П-1000К → {44502, 44550}; П-26/800ЭР →
  {44510, 44511}; П-30/900ЭР → {44512, 44513}; П-26/800ЭР-2 → {44510, 44511};
  **многие-к-одному**: карточки «П-24/700ЭР» и «П-24/700ЭР-2» → один товар
  44506 (обе в отчёт, см. §3).
- dropped 26 — все на **not_found** карточках (24 «поле отсутствует в карте» —
  аккумуляторные карточки Ресанты/ВИХРЯ и варианты подписей у новых моделей,
  §8 п. 4; 1 fallback без «Вт» в сводке аккумуляторной; 1 значение опции).
- not_found 34 — модели, которых нет в нашем каталоге (в т.ч. аккумуляторные
  АП-20Li/4 Ресанты и ВИХРЯ), плюс ЗП-2890 м (наш 44527 без суффикса «м» в
  названии — известное из PARS-04, §8 п. 3).

## 3. Изменение импортёра по ходу фазы

Сухой прогон вскрыл дыру: две карточки («П-24/700ЭР» и «П-24/700ЭР-2»)
матчились на один товар 44506 и их значения смешались бы в записи. Правило
«многие к одному — в отчёт, не в базу» теперь enforced в
`catalog_import_scraped` (коллизия карточек на товар → ambiguous, план
отменяется) + регрессионный тест `test_many_cards_to_one_product_not_written`.
Сьют: **9 passed**; ruff/black чисто.

## 4. Гейт-цикл (локально)

1. **Preflight**: отпечаток неприкасаемых полей 21 товара
   (`1e80327d…` → `phase5-fingerprint-before.txt`); `content_locked` — 0;
   PAV `manual`/`import_1c`/`rules` по управляемым атрибутам в скоупе — 0.
2. **pg_dump** до записи — `phase5-predump.dump` (custom, 4,0 МБ).
3. **Предсказание**: create 13, overwrite 0 → записей 13.
4. **Write**: факт = предсказанию — **13 PAV**, транзакция на товар,
   ImportRun #45.
5. **Post-audit**: отпечаток идентичен (`1e80327d…`); дублей PAV — 0;
   `attrs_cache ≡ EAV` по 6 затронутым; PAV `scraper` = 68 (55 + 13);
   `voltage` у сетевых в скоупе — 0; повторный прогон — **0 записей**,
   106 confirm (ImportRun #46).
6. **Витрина живым запросом**: `GET /api/catalog/products/0110000379/`
   (44539, ЗУБР П-24-750) → 200, в карточке появились «Обороты х.х. 870 об/мин»
   и «Энергия удара 2.4 Дж» — значения этого прогона.

## 5. Эффект для покупателя (замер через код витрины `build_facets`)

Файлы: `phase5_facets_before.json` / `phase5_facets_after.json`.

| Метрика (видимые перфораторы, 140) | до | после | Δ |
|---|---|---|---|
| с `power` | 119 | 119 | +0 |
| с `energy_impact` | 118 | 119 | **+1** |
| с `no_load_speed` | 32 | 35 | **+3** |
| с `chuck` | 122 | 122 | +0 |
| с `motor_type` | 8 | 8 | +0 |
| с `power_source` | 131 | 131 | +0 |
| с `voltage` (аккумуляторные) | 10 | 10 | +0 |
| фасет «Мощность»: значений / сумма count | 32 / 119 | 32 / 119 | +0 |
| фильтр «Перфораторы 800–1000 Вт» | 58 | 58 | +0 |

**Честная рамка.** Этот прогон — прежде всего **подтверждение** существующих
regex-значений спецификацией производителя (93 confirm) и полный охват
актуальных каталогов четырёх брендов. Прирост фильтруемости скромный, потому
что: (а) большинство matched-товаров уже имели характеристики из regex;
(б) 3 из 6 товаров с новыми значениями — `is_active=false` (44507, 44538,
44540): их EAV заполнен, но на витрину они не выходят, поэтому фасеты не
сдвинулись. 44507 ИНТЕРСКОЛ П-32/1100АВС получил **полный набор** (power 1100,
energy_impact, no_load_speed, chuck, motor_type, power_source) — после
публикации он станет фильтруемым по мощности (§8 п. 1).
Совокупно за PARS-04+05 записано **68 PAV `scraper`**, из них у видимых
товаров: chuck 8/9, energy_impact 13/15, motor_type 1/2, no_load_speed 19/22,
power 9/10, power_source 9/10. Потолок затеи на текущих каталогах брендов
достигнут: оставшиеся 21 видимый перфоратор без `power` — это Bosch/DeWalt/
HiKoki/Einhell/Hanskonner и снятые с производства модели, которых нет на
четырёх сайтах.

## 6. Что не тронуто

Цена, остаток, `code_1c`, `article`, `name`, дерево, `tool_type`,
`manual`/`rules`/`import_1c` — вне записи (отпечаток + skipped_priority=0).
Фото не брались. Новых Attribute/Option не создавалось (несопоставленное — в
отчётах `dropped`/`unmapped_attributes`). Контур `tool_type` не тронут.
Глобальные команды не запускались; кэш пересобран точечно. Push/PR нет.

## 7a. Staging — гейт-цикл по GO владельца (2026-07-29)

GO получен; окно сверено (TT-07/CAT-05 не писали одновременно — деплой PR #611/#612
завершился до начала прогона). Код на staging — из git (PR #612, включает фикс
«многие к одному»), миграции 0028+0029 применены.

**Расхождение цифры GO.** GO цитировал «13 значений на 7 товаров» — это локальный
остаток после PARS-04. Dry-run на staging предсказал **62 записи (61 create +
1 overwrite) на 21 товар**, потому что staging не получал локальных записей и его
каталог беднее. Сверено с владельцем перед записью: арифметика сошлась —
недостающие 6 пар = товар 44506, который после фикса «многие к одному» обоснованно
исключён (61 + 6 = 67 = локальные create PARS-04+05); skipped_priority = 0,
лишних пар нет, наборы товаров совпадают.

**Цикл (все числа предсказаны dry-run'ом до записи и сошлись):**

1. Фасеты «до» — `phase5-staging-facets-before.json`.
2. Dry-run: matched 21, ambiguous 6, not_found 34 — как локально; create 61,
   overwrite 1 (44544, 5,5 → 5,2 Дж — подтверждённый прецедент), confirm 44,
   skipped_voltage 20, dropped 26. Отчёт — `phase5-staging-dryrun.json`.
3. Preflight: отпечаток 21 товара `1e80327d…` (совпал с локальным),
   content_locked = 0, manual/import_1c/rules в скоупе = 0.
4. pg_dump — `/tmp/phase5-staging-predump.dump` на VPS (custom, 4,0 МБ).
5. **Write: факт = предсказанию — 62 PAV** (ImportRun #45).
6. Post-audit: отпечаток идентичен; дублей PAV — 0; `attrs_cache ≡ EAV` по всем
   21; PAV `scraper` = 62; `voltage` в скоупе = 0; 44544 → 5.2/scraper;
   повторный прогон — **0 записей** (ImportRun #46).
7. Витрина живым запросом: `GET https://dev.proff58.ru/api/catalog/products/0110000379/`
   (44539, ЗУБР П-24-750) → 200: «Обороты х.х. 870 об/мин», «Энергия удара 2.4 Дж»,
   «Мощность 750 Вт», «Питание Сеть», «Тип патрона SDS-plus»; voltage отсутствует.

**Эффект для покупателя на staging** (видимые перфораторы, 140):

| Метрика | до | после | Δ |
|---|---|---|---|
| с `power` | 110 | 118 | **+8** |
| с `energy_impact` | 107 | 118 | **+11** |
| с `no_load_speed` | 16 | 34 | **+18** |
| с `chuck` | 114 | 121 | **+7** |
| с `power_source` | 122 | 130 | **+8** |
| с `voltage` (аккумуляторные) | 10 | 10 | +0 |
| фасет «Мощность» (сумма count) | 110 | 118 | **+8** |
| фильтр «Перфораторы 800–1000 Вт» | 54 | 58 | **+4** |

На staging эффект заметнее локального: там слоя regex-обогащения не было,
поэтому **8 перфораторов стали фильтруемы по мощности впервые**, а фильтр
«800–1000 Вт» вырос с 54 до 58 товаров.

## 8. Требует решения владельца

1. **Публикация 44507, 44538, 44540** (`is_active=false`): 44507 получил
   полный набор характеристик — после активации он станет фильтруемым по
   мощности. Это единственный «новый фильтруемый по power» товар прогона.
2. **Модерация дублей** (из ambiguous): П-1000К {44502, 44550}, П-26/800ЭР
   {44510, 44511}, П-30/900ЭР {44512, 44513}, и многие-к-одному 44506
   (карточки П-24/700ЭР и П-24/700ЭР-2 — какая ревизия канонична). После
   модерации повторный прогон заполнит их без изменений кода.
3. **44527 «ЗП-2890 м»** — переносится из PARS-04 (суффикс «м» отсутствует в
   нашем названии; article бы подтвердил, но ключом запрещён).
4. **Аккумуляторная ветка карты**: в полном сборе появились карточки с полями
   «Напряжение аккумулятора, В», «Емкость аккумулятора, А*ч», «Тип двигателя»
   (у resanta/vihr), «Номинальная мощность» — под все есть существующие
   атрибуты (`voltage`, `battery_capacity`, `motor_type`, `power`). Расширить
   `scraped_attr_map.perforatory.json` этими полями? Записывать было бы
   некуда (карточки not_found), но карта станет готовой к аккумуляторным
   моделям в нашем каталоге.
5. **not_found 34** — потенциальный вход для закупки (П-750/П-850К ВИХРЯ,
   АП-20Li/4, линейка Интерскола и др.).
6. **Staging**: GO не запрошен. При GO — повторить гейт-цикл на staging с
   теми же предсказанными числами, предварительно сверившись с окнами TT-07 и
   CAT-05 (одновременные write на staging запрещены).
