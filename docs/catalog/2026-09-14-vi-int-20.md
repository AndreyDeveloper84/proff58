# VI-INT-20: RESEARCH_COMPLETE

Image Opportunity Research / Wave 2B–2C Replanning — итоговый отчёт.
Режим: **READ-ONLY / RESEARCH ONLY / NO COLLECTOR** — соблюдён полностью:
ноль source requests (включая «проверочные»), ноль writes на staging (все
скрипты — SELECT-only с guard `current_database()=='proff58_staging'`),
collector не запускался, git не трогался.

Отчёт покрывает пп.1–20 промпта владельца; дополнительные секции 21+ мне
не переданы — если существуют, не покрыты.
*(Примечание VI-INT-22: последующие owner requirements были разрешены
итоговым decision/recommendation и исполнением VI-INT-21; VI-INT-20 не
выполнял source requests/writes. Никаких исследований задним числом
не добавлялось.)*

Артефакты: `/home/taximeter/vi02/20-*` + локально `scratchpad/vi-int-02/20-*`
+ docs-копия `docs/catalog/2026-09-14-vi-int-20.md` (worktree vi-int01,
незакоммичено — будущий docs closeout).

## 1. Frozen starting baseline (п.1) — `20-baseline.json`

- Deployed SHA (host checkout `/home/taximeter/proff58-staging`):
  `8a51fbf3fd9406395f05eeea570c8b2586f6a19a` (merge PR#766
  docs/vi-int-18-infra-closeout; в контейнере git отсутствует — зафиксировано).
- Product total 47225 · ProductImage **805** ✓ · PAV **84839** ✓ ·
  Attribute **110** ✓ · Option **675** ✓ · CategoryAttribute **298** ✓ ·
  migrations **128** ✓ — все совпали с ожиданиями.
- ProductImage source dist: huter 62 · manual 107 · resanta 298 · vihr 108 ·
  vseinstrumenti 230.
- **Remaining long-tail = 58, ожидание 58, drift 0** (64 на VI-INT-16 минус
  6 закрытых VI-INT-18). Drift-разбор не потребовался.

## 2. Predicate long-tail (п.2) — `20-predicate.md`

EXACT predicate (дословный код из `04-scope-discovery.py` / `11-recount.py`,
независимо воспроизведён `20-baseline.py`):

```
pool        = Product(is_active=True, available_quantity>0)          # 6148
tail_types  = {tool_type: count в pool ∈ 1..5}                       # 116 типов
identity_A  = reliable(article) AND brand≠'' AND norm(article) глобально уникален
remaining   = pool ∩ tail_types ∩ identity_A ∩ images==0             # 58
```

`reliable` = norm len≥3 + есть цифра; `norm` = uppercase alnum; уникальность —
по всем Product, не только pool. История: 128 → 70 → 64 → **58**.

## 3. Inventory 58 (п.3) — `20-inventory.json` (+ `20-inventory-db.json`)

58 строк: product_id | article | brand | title | original_name | category
(id/name/slug) | tool_type | active | stock | image_count | main_count |
pav_count + sources | content_locked | processing_items | moderation flags |
identity_class | research_state | source_evidence.

Сводка: images суммарно 0 (консистентно predicate); content_locked 0; с
processing-историей 20; открытые needs_review у 5: 35608 (review),
43204 (unknown), 43994 (unknown), 44910 (**rejected**), 44911 (unknown) —
все по tool_type-исследованиям, ни один не по image identity.

## 4. Классификация 58 (п.4) — `20-classification.json`

| Класс | Число | Состав |
|---|---|---|
| SOURCE_BLOCKED | **17** | SKM, retry_justified=false: pilot-30 (12956, 35608, 11092, 5875); run-1 (651, 43832); run-1+2 (11093, 4665); run-2 (13004, 44849, 44850); research-16 (7335, 44181); wave-2a (45480, 45483, 45486, 45488 — класс ЗШ-П) |
| NO_RESEARCH_YET | **39** | ни один run не исследовал; identity: I1×5 (43824, 43994, 44698, 44910, 44911), I3×34 |
| RESEARCHED_NO_REFS | **1** | 22673 |
| AMBIGUOUS_IDENTITY | **1** | 42072 (артикул 20805 общий числовой, чужие карточки, ~12–16 loads > budget 9 — UNRESOLVABLE_IN_BUDGET) |
| NON_STRICT_EVIDENCE_EXISTS | 0 | см. п.19 |
| MODERATION_REQUIRED | 0 | как первичный класс (флаги needs_review — по tool_type, см. п.3) |
| OTHER | 0 | |

Сумма 58 ✓.

## 5. 22673 (п.5)

- Strict card на диске с batch-a run-2: `article_exact+brand_exact`,
  vseinstrumenti product 1185279, queries «76430», «76430 СЕРВИС КЛЮЧ».
- `images=[]`, `image_refs=[]` — **единственный из 18 matched run-2 без refs**;
  поле ошибки отсутствует, причина не зафиксирована (вероятно: галерея
  карточки в момент прогона не отдала валидные изображения extractor'у).
- Источники: только vseinstrumenti (единственный источник контура).
- Повторный collector: единственный осмысленный кандидат среди 58 (URL
  известен, strict identity подтверждена, ожидание ~1–3 refs) — но collector
  запрещён в этом треке. **Остаётся RESEARCHED_NO_REFS до отдельного GO на
  re-extract.** Искусственно не искали.

## 6. Wave 2B / gravery (п.7–8) — `20-gravery.json`

- Границы: категории «*грав*» **не существует** — граница только tool_type
  (16/17 в категории «Электроинструмент» id 3; 28919 — «Пневмоинструмент»
  id 162). Taxonomy не мигрировали.
- Universe tool_type=gravery: **17** products; с images 3 (43836/43837/43838);
  в remaining 58 — **43824** и **43832**; в pool ещё 28919 (без артикула —
  unmatchable); 13 вне pool (no stock/inactive).
- Evidence: 43824 — DREMEL `DRM-F0130290JZ`, I1, никогда не исследовался
  (потенциально strict-matchable, требует нового сбора); 43832 — ЗУБР
  `ЗГ-160 КН41`, SKM batch-a run-1, retry не оправдан (blocked).
- Основание Wave 2B (VI-INT-12): «43824 — типовой strict-кандидат; +1 в
  следующий combined track, НЕ отдельный wave» — **подтверждено**.

## 7. High-ROI types (п.9) — `20-high-roi.json`

11-decision-table восстановлена и пересчитана (32 типа remaining). Закрыты
волнами: boltorezy 3/3, aksessuary-shg, bp-podgotovka, bruski-shlif (VI-INT-18),
stameski/stanki(частично)/sverlilnye/ustanovki-almaznogo/payalniki
(wave-2a→VI-INT-13/16). Global hit-rates: pilot-30 25/30 strict · batch-a
run-1+2 41 strict (8 SKM + 1 conflict) · research-16 13/16 (81%) · wave-2a
6/10 (60%; без shtoborezy 6/6 = 100% на I1). Per-type hit-rate для 22
неисследованных типов — **GAP** (помечено, не выдумано).

## 8. Wave 2C пересмотр (п.10–11) — `20-wave2c-review.md`

- Strict exhaustion подтверждён: в 58 нет ни одного продукта со strict card и
  refs>0.
- I4/SKM (14): история 0/17 исследований + 2 conflict + 1 unresolvable —
  повтор нерационален.
- I3 no-research (34): отрицательно отобранный остаток; прецеденты 2621/7335/
  42072; ожидание ≤25% (GAP-оценка); ~122 loads на слой. Слоем — нерационально.
- **shtoborezy: рекомендовано исключить явно** (wave-2a 4/4 SKM, класс ЗШ-П
  источником не покрыт).
- Вывод: Wave 2C как волна **не рационален** — решение VI-INT-12 подтверждено.

## 9. Image-first + opportunity score (п.12–14) — `20-opportunity-score.json/.md`

Image-first подтверждён как стратегия (наибольший yield/продукт во всех
волнах), но для 58 готовых refs нет — дальше только collector/re-extract.
Компоненты раздельно (coverage / identity / historical yield / moderation /
complexity / strategic), без скрытой формулы. Топ-5:

1. **43824 gravery** — I1, DREMEL, moderation 0, закрывает основание Wave 2B.
2. **44698 steplery-i-zaklepochniki** — I1, moderation 0.
3. **43994 drovokoly** — I1, закрывает тип целиком; needs_review(unknown).
4. **44911 stanki** — I1, сосед 44908 MATCHED (refs 3); needs_review(unknown).
5. **44910 stanki** — I1, но needs_review=**rejected** → сначала модерация.

Не рекомендованы: I3-слой (только точечно), 2C I4/SKM + shtoborezy (исключить).

## 10. Packages B/C/D review (п.15–17) — `20-packages-review.json`

Пакеты VI-INT-15 — про характеристики, не images; **не применять**.

- **B (S2=4)**: select-значения вне словаря (24740, 33252, 42549, 22673).
  Strict не при чём — это option-нормализация. Решаемо маленьким map-patch
  треком, collector и модерация не нужны. 3/4 продуктов уже с images.
- **C (S3=7 значений / 5 entries)**: per-tool_type семантика (Тип/Длина) —
  держать только вместе с решением по schema v2; отдельно приоритет низкий.
- **D (S4=9)**: 9 новых canonical attributes ради 9 значений — самый дорогой
  класс; не запускать без отдельного schema-трека.

## 11. Existing non-strict evidence (п.19) — `20-nonstrict-evidence.json`

Все классы = **0** (exact model / normalized model / alias / model-only /
fuzzy/ambiguous / source conflict). Все persisted matches всех runs — strict:
126×`article_exact+brand_exact` + 1×`+model_confirmed` (15-all-matches —
aggregate, пересекается, в сумму не входит). Отклонённые кандидаты никогда не
персистились как refs (fail-closed), существуют только как счётчики в
run-reports. IDENTITY_CONFLICT (2620, 2624) — вне remaining 58.

## 12. Identity gate (п.20)

**Ослабление НЕ рекомендуется** — model-only/fuzzy не вводить. Gate доказал
обратимость и чистоту attribution во всех волнах (VI-INT-13/16/18: global
delta = exact ours, foreign 0 везде). Зафиксировано в
`20-opportunity-score.json` (`identity_gate_p20`) и шине.

## 13. Recommendation + exact proposed scope следующего track

**Рекомендация: combined micro-track (image-only), по отдельным GO на каждый
элемент:**

1. **GO-1 — collector strict для 43824** (gravery, DREMEL DRM-F0130290JZ):
   единственный I1 no-research без moderation-флагов и с сильным брендом;
   закрывает основание Wave 2B. Прогноз ~3.6 loads, ожидание ~3 refs.
2. **GO-2 (опционально, независимый) — re-extract refs для 22673**: strict
   card + URL уже на диске; collector-запрос только на известную карточку
   (без search), ожидание ~1–3 refs.
3. После GO-1/GO-2, при refs>0 — apply по отработанному протоколу VI-INT-18
   (frozen manifest + sha256 gate, drift gate, pilot, audits, rerun).

**Обоснование:** это единственные два продукта из 58 с ненулевым обоснованным
ожиданием: 43824 — I1 без истории промахов (I1 hit-rate 100% на wave-2a без
shtoborezy), 22673 — strict identity подтверждена, refs не извлечены по
технической причине. Остальное: 17 SOURCE_BLOCKED (0/17 история), 42072
AMBIGUOUS, 34 I3 (≤25% оценка, дорого), 4 I1 с moderation-флагами
(43994/44911 — unknown; 44910 — rejected: collector до разрешения маркеров
не запускать).

**Не предлагать:** Wave 2B standalone (universe pool = 1 реальный кандидат),
Wave 2C (нерационален), shtoborezy (исключить), Package B внутри image-track
(это map-patch, отдельный data-only трек без collector).

## Verdict

**RESEARCH_COMPLETE.** Baseline зафrozen (58, drift 0), все 20 пунктов
закрыты артефактами, запреты соблюдены. Следующий шаг — за OWNER
(GO-1 / GO-2 / иное).
