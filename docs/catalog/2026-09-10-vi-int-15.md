# VI-INT-15: RESEARCH_COMPLETE

Existing Evidence Cleanup Research — финальный отчёт. Всё read-only: staging
writes — нет, map/schema/scope — не менялись, collector — нет, config changes —
нет, git — нет. Артефакты: `/home/taximeter/vi02/15-*` + `scratchpad/vi-int-02/15-*`.

## 1. Цель

Классификация существующей evidence (3 группы: A — Wave 2A strict 6; B — old
unused strict 6; C — 22673) по S1–S5 и детерминированный proposal.

## 2. Revalidation (п.2)

13/13 проверено, блокирующего drift нет (`15-revalidation.json`): B и C clean
(article/brand/tool_type совпадают, strict cards принадлежат продуктам,
content_locked False, images 0); A — 6 «drift images>0» = ожидаемое состояние
(images VI-INT-13); 26863 — исторический needs_review июля (не блокер).

## 3. Deployed contract (п.3)

`15-deployed-contract.json`: SHA e9a2f990, map sha256 90c49e2a…, rules 260c7852…;
scope=40; 16 mapped полей дословно; все canonical Attributes существуют;
material options 22 (вкл. crv-steel/polipropilen); source_priority
manual/rules/import_1c > scraper.

## 4. Evidence table (п.4)

137 значений, production `extract_card_values` на deployed:
**S1: 16 · S2: 4 · S3: 7 · S4: 9 · S5: 101** (`15-evidence-table.json/.md`).

## 5. Group A сводка

Aggregate raw-map **15 PAV подтверждён 1:1** на текущем deployed state
(24740:3, 33229:1, 44908:5, 44928:4, 44985:2) — все 15 = S1 (scope-locked);
deployable CURRENT = 0 (типы вне scope).

## 6. S1 evidence chains (п.6)

16 цепочек `field → map[field] → canonical attr → value` с доказательством
работающих маппингов (`15-s1-chains.json/.md`): power (44144/44145: 1400/1600),
weight_kg (643/1782/…), voltage 18.0 (643/519), width (36919/36922),
height (35081), diameter (41826), bore/disc (44888+), torque (13002),
piece_count (22673: 7.0 уже в БД!), drive (12957/12959/13002), material
(crv-steel/polipropilen/steel).

## 7. Generic-field audit (п.7)

Ни один generic не повышен до S1: «Тип»/«Длина» → S3, «Класс товара»/«Цвет»/
«Механизм»/«Min усилие» → S4, «Тип инструмента»/«Назначение»/«Объем» → S5.
Fail-closed сохранён (`15-audits.json`).

## 8. Unit audit (п.8)

Все 16 S1 — прямые единицы, конверсия не требуется (Вт/В/кг/мм/шт напрямую).
Особо: **Напряжение — не CREATE, а PROTECTED**: правило проверено в коде
(`scraped_import._plan`: voltage на не-battery товаре (≥60В и нет
power_source=battery) → skipped_voltage всегда; батарейные 18В пишутся).

## 9. Select audit (п.9)

В 16 S1 select-значений нет (все decimal); resolution по deployed словарю
однозначен; новых options не требуется. S2-select (Питание/Материал/Покрытие) —
вне S1.

## 10. Group B сводка

raw-map 0, deployable 0; исторические причины актуальны (0-safe-PAV simulation;
boltorezy реабилитирован «Длиной», но это S3/schema v2, не S1).

## 11. Group C (22673) сводка

piece_count=7 **уже применён** (scraper 7.0000 в БД — фаза 1 ошибочно дала
yield=1 из-за сравнения строк '7' vs '7.0000'; исправлено по факту из БД).
Остающийся safe yield = **0**, confirm 1. Images: refs отсутствуют.

## 12. Scope expansion simulation (п.14)

In-memory, production extract, без изменений конфигурации
(`15-scope-simulation.json/.md`): additions payalniki, stameski,
stanki-derevoobrabatyvayushchie, sverlilnye-stanki, ustanovki-almaznogo-bureniya →
**CREATE 11, CONFIRM 1, PROTECTED 4 (skipped_voltage), conflicts 0**.

## 13. Blast radius (п.15)

Total products в 5 типах: **118** (stameski 55, sverlilnye-stanki 32,
payalniki 21, ustanovki 7, stanki-derevoobrabatyvayushchie 3). Узкие — только
stanki-derevo (3) и ustanovki (7). Операционный факт: scope addition действует
на тип целиком; unexpected candidates = 0 на текущих выгрузках (importer пишет
только по картам файла выгрузки).

## 14. Source-protection simulation (п.16)

manual/rules/import_1c не перезаписываются (source_priority); prune/collateral =
0; attrs_cache — ожидаемый delta = пересчёт `_cache_from_pavs` (тот же аудит,
что в VI-INT-03/04A/07/09, mismatch 0).

## 15. Regression surface (п.17)

`test_vi_pack_a::test_pack_a_scope_additions_present` ожидает ровно scope=40 —
требует обновления до 45 (+5 типов). Новый тестовый файл по образцу: extraction
карт 5 новых типов (fixture), negative skipped_voltage ×4, scope-out тип
(boltorezy) не матчится. Unrelated types — не затрагиваются.

## 16. S1 ROI (п.18)

**11 CREATE на 5 товаров** при data-only diff (+5 scope строк), конфликтах 0,
PROTECTED-правило сохранено. Complexity ~нулевая; regression surface = 1
обновление + 1 файл. Blast radius wide-типов — операционный факт, не блокер.

## 17. Package A — PURE SCOPE EXPANSION

`15-decision-packages.json/.md`: exact diff (+5 scope_tool_types), yield по
типам с PROTECTED, blast radius.

## 18. Package B — MAP WORK (S2, 4)

Питание «от сети» (alias), Материал «пластик» (option), Питание «механическая»,
Покрытие «нет».

## 19. Package C — SCHEMA V2 (S3, 7) + future evidence

«Тип» (payalniki, bruski-shlif, nabory-klyuchey), «Длина» (boltorezy ×3,
sverlilnye-stanki). Future: «Тип патрона» sds-max (44177, 1 PAV),
«Длина» mm boltorezy (4 PAV), Плавный пуск/Регулировка оборотов (4 PAV).

## 20. Package D — NEW ATTRIBUTES (S4, 9)

Плавный пуск ×2, Регулировка оборотов ×2, Материал рукояти ×2,
Материал обработки ×2, Трещотка ×1.

## 21. Package E — NO ACTION (S5, 101)

Шум/дубли/confirm/нет однозначного canonical.

## 22. Old strict cleanup package

По 6 old strict + 22673: chars_safe_now = 0 у всех; image refs 3×6 лежат
(кандидаты image-only track по отдельному OWNER GO); 22673 — chars закрыты
(confirm), refs нет. НЕ execution manifest.

## 23. Честные исправления фазы 2

- skipped_voltage: все 4 voltage-значения (включая '220/230') → PROTECTED по
  точному правилу кода (не по строке raw).
- 22673: piece_count уже применён (строковое сравнение '7' vs '7.0000' дало
  ложный yield=1 в фазе 1 — исправлено по факту из БД).

## 24. Recommendation (п.24)

**R1 — IMPLEMENT PURE SCOPE EXPANSION.** 11 CREATE при data-only diff, нулевых
конфликтах, доказанных маппингах, защищённом voltage-правиле, минимальной
regression surface; wide-type blast radius (stameski 55, sverlilnye 32,
payalniki 21) — принять как операционный факт (типы и так в long-tail roadmap,
importer пишет только по собранным картам). Secondary: old strict cleanup — НЕ
сейчас (chars_safe_now=0); image refs 3×6 — кандидаты image-only track по
отдельному GO.

## 25. Связь с VI-треком

VI-INT-03/04A/06/07/09/13 — applied evidence (scraper-PAV доказательства для
chains); 05/06 — Pack A; 08 — S3 inventory (уже упакован); 11 — cost model;
12 — Wave 2A; 13 — images applied.

## 26. Риски

Wide-type scope additions меняют площадь importer'а для будущих сборов — смягчено
тем, что запись идёт только по картам выгрузки; voltage-ловушка закрыта кодом;
skipped_voltage обязан остаться PROTECTED в будущем apply (negative fixture
готов).

## 27. Что НЕ делать

Scope expansion вне Package A; map changes (S2) без отдельного решения;
schema v2 (S3) — defer; new attributes (S4) — research later; S5 — никогда.

## 28. Артефакты

`15-*` (12 файлов): revalidation, deployed-contract, evidence-raw,
evidence-table(.md), group-summaries, s1-chains(.md), audits,
scope-simulation(.md), regression-roi, decision-packages(.md), report.md.

## 29. Отклонения/оговорки

Фаза 1 Group C yield=1 — ошибка сравнения строк, исправлена (факт: 0, confirm 1).
Фаза 2 voltage — исправлена с raw-string на точное правило кода.

## 30. Вердикт

**VI-INT-15: RESEARCH_COMPLETE.** 137 значений классифицированы (S1:16, S2:4,
S3:7, S4:9, S5:101); Package A (pure scope expansion, +5 типов, 11 CREATE)
готов к OWNER GO; остальное упаковано (B/C/D/E). Ничего не применялось.

После отчёта — стоп.
