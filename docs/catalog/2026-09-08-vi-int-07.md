# VI-INT-07 — Controlled Batch B (evidence-bounded, 15 товаров) → **PASS**

Дата: 2026-09-08. Продолжение `2026-09-08-vi-int-06.md`. Batch B выполнен
строго из уже собранных strict-карточек: **новый collector НЕ запускался**,
размер определён eligibility (15), а не первоначальной лестницей «40».
**Track 2 = NOT STARTED.** Данные остаются на staging.

## Вердикт: PASS

- identity strict 100 % · false assignment 0 · conflicts 0 · unit-trap 0 ·
  semantic mismatch 0 · schema expansion 0 · taxonomy write 0 ·
  higher-priority overwrite 0 · existing mutations 0 · duplicate main 0 ·
  duplicate images 0 · orphan files 0 · foreign mutations в scope 0 ·
  rerun = полный no-op (downloads 0).

## 1. Актуальный remaining scope (пересчёт на день запуска)

- pool 6148 (было 6150 — 2 выбыли из пула), tail types 117, **remaining
  long-tail = ровно 101** (20 Batch A выбыли по фото, +2 по пулу
  компенсировались). Deployed scope = 40 типов.

## 2. Drift относительно VI-INT-05/06

**Нет.** Фактические числа совпали с baseline expectation: remaining 101,
strict+covered eligible 15 (14 с safe PAV + 4820 image-only) — 1:1 с
«strict collected covered 15» simulation.

## 3. Eligibility matrix

По каждому из 101: product_id | active | tool_type | map_in_scope |
source_card | identity | existing_PAV | existing_images | main_count |
other_track | eligibility — `07-scope-recompute.json`. **Eligible = 15**:
1782, 2916, 4667, 4820, 7326, 13002, 22673, 28587, 35805, 36920, 36921,
40820, 44142, 44175, 44388.

## 4. Причины exclusions (с пересечениями классов)

`no_source_card` 80 · `tool_type_out_of_deployed_scope` 65 ·
`skm_or_conflict` 15 · inactive 0 · content_locked 0 · other active track 0
· staging drift 0. Обогащённые 25+20 — вне long-tail (фото); Pack A-добор
на них (+11/+3) в Batch B не входил.

## 5. Immutable Batch B manifest + SHA256

`07-batch-b-manifest.json`, 15 товаров, deterministic ordering,
**SHA256 `ce4c4f929d73f46913c9af7282b0b26ac122bb411a95a6f2ee73f01343fca116`**
(runner проверял при каждом write — `MANIFEST_SHA256_OK`). 38 image_refs,
24 expected char operations. После freeze состав не менялся.

## 6. Characteristics dry-run

matched **15/15**, ambiguous 0, not_found 0. **CREATE 23** (weight_kg 11,
material 4, piece_count 2, width 2, power 2, torque 1, drive 1 — 14
товаров) · ALREADY_PRESENT/confirm 1 (44175 power 1500, source=regex —
защита) · DROPPED_SELECT 68 (66 «поле вне карты» + 2 «вне словаря») ·
UNMAPPED 39 · CONFLICT 0 · SKIPPED_VOLTAGE 0. Все гейты зелёные; DB drift
после dry-run = 0.

## 7. Фактический вклад Pack A

Offline replay production-кодом на тех же 15 картах: карта origin/dev →
**3 товара / 4 PAV**; карта Pack A → **14 товаров / 24 PAV**. Дельта:
**+11 товаров, +20 PAV** — из них 12 PAV существующими полями на новых 10
scope-типах, crv-steel 3, piece_count 2, polipropilen 1, torque 1, drive 1.
Simulation VI-INT-05 подтверждена фактом 1:1.

## 8. Fail-closed audit «Тип»/«Длина»/units

FAIL_CLOSED_OK: generic «Тип» — ни одного импорта; «Тип инструмента» —
dropped ×3 (не PAV, taxonomy цела); «Длина» — unmapped, включая ленты
«30/50/100 м» (метры→мм конверсии нет, length/tape_length в created
отсутствуют); «Min усилие» '28' — dropped, НЕ torque.

## 9. Image preflight

refs 38 (22673 — 0 refs, image-only; median 3, max 3) · existing images 0
у всех 15 · existing main 0 · source_url collisions 0 · input дублей 0 ·
invalid (M-13) 0 · expected downloads 38.

## 10. Backup + checksum + structural validation

`db-20260907-220803-pre-vi-int07.dump` (22 272 941 б), sha256
`0ca80dba…e763`; exit 0; TOC 733 / TABLE DATA 72; обязательные
catalog-таблицы на месте; persistent storage; df ≥2 ГБ.

## 11. Baseline

B4 `07-baseline-B4.json`, SNAPSHOT_SHA256 `e83589f1…a611` — непосредственно
перед write (15 id + globals).

## 12. Recovery plan

`07-recovery-plan.md`: provenance-based. PAV — exact created set
(product_id, attribute, source='scraper') + обязательное пустое
пересечение с B4; images — `catalog_images_ops --source vseinstrumenti
--since <окно>` + foreign records = 0. Без диапазонов id.

## 13. Pre-write drift gate

**CLEAN**: 15/15 без изменений против frozen manifest (active, stock,
tool_type, article, images, PAV, content_locked) —
`07-pre-write-drift-gate.json`.

## 14. Characteristics apply

`ЗАПИСЬ (PAV: 23)` — matched 15/15. Guard: proff58_staging +
VI03_STAGING_PILOT=1.

## 15. Characteristics post-audit — ALL_OK

created **23 == dry-run плану точно** · confirm 1 (защита regex) ·
existing updates/deletes 0 (программный diff vs B4) · все новые
source='scraper' · conflicts 0 · schema delta 0 · taxonomy delta 0 ·
attrs_cache 15/15 · ImportRun 103 (журнал). Инцидент: сторонний перезапуск
proff58_staging-web-1 в 19:11:28 UTC между apply и post-audit'ом — apply
завершился до рестарта, целостность подтверждена после восстановления
(healthy).

## 16. Images apply

Существующий `ImagePipeline.process_batch` (код не менялся). Пилот 13002 →
полный прогон: created 35 + dedup 3 = **38/38**, rejected 0.

## 17. Image post-audit — ALL_OK

affected ⊆ 15 · source=vseinstrumenti · provenance полный
(source_url/checksum==sha256 файла/fetched_at) · dup (product,source_url)
= 0 · dup (product,checksum) = 0 · existing mutations 0 · **main: 14
товаров ровно 1 (первое созданное), 22673 — 0 (норма), main>1 = 0** ·
orphan files 0 · ordering (-is_main, sort_order, pk).

## 18. Mandatory rerun — полный no-op

chars: `ЗАПИСЬ (PAV: 0)` create 0 / update 0 / confirm 24 (ImportRun 106)
· images: created 0, все 38 dedup **до сети** (downloads 0, окно 0,12 с) ·
main flags побайтово не изменились · `migrate --plan` пуст.

## 19. Final post-audit — ALL_OK

По 15 товарам: PAV 19→42 (+23), images 0→38 · main distribution 1/14/0 ·
schema counts == B4 побайтово (Attribute 100, Option 601) · source_dist:
vseinstrumenti **159** (121+38), прочие без изменений · контейнеры
healthy.

## 20. Фактический PAV yield

**23 PAV** (14 товаров): weight_kg 11, material 4, piece_count 2,
width 2, power 2, torque 1, drive 1.

## 21. Фактический image yield

**38 изображений** (14 товаров с refs; 22673 — характеристики без
изображений).

## 22. Products meaningfully enriched

**14** (13 обеими частями, 4820 только изображениями, 22673 только
характеристиками).

## 23. Remaining long-tail после Batch B

101 − 15 = **86** (все 15 выбыли из long-tail: 14 получили фото, 22673 —
характеристики без фото, остаётся в пуле до фото — уточнение: выбывшие по
фото 14, пул-факт пересчитается при следующем scope discovery).

## 24. Remaining strict cards on disk

**0 in-scope unused.** Все 24 strict in-scope карточки использованы
(20 Batch A + 4 Batch B-вошедших из бывших «готовых»; 15 Batch B — полный
расход eligible). Дальнейшее enrichment — только через новый сбор
(отдельное решение) или Track 2.

## 25. Новые GAP

- **G-4 (наблюдение, не дефект):** staging активно работают параллельные
  треки (enrich keyword: +414 PAV за то же окно, чужой перезапуск web).
  Атрибуция наших изменений доказуема per-product diff + source-разрезом —
  протокол атрибуции обязателен и дальше.
- По коду/данным новых GAP нет.

## 26. Pre-existing load_attributes drift — untouched

1 attribute update + 8 bindings **не применялись, не чинились, не
входили в Batch B**; полный `load_attributes` не запускался.

## 27. Track 2 = NOT STARTED

## 28. New collector = NOT STARTED

## 29. Recommendation следующего шага

- **Short-term:** characteristics-потенциал текущих карт исчерпан
  (eligible 0). Следующий characteristics batch требует нового сбора —
  естественный вход — **targeted collector pool 16** (Track 2, готов,
  ~51 loads) или сбор по top-ROI типам из `05-tooltype-roi` (boltorezy 5,
  izm-dalnomery 5, siz-nakolenniki 5 — карточек нет).
- **Image-first remainder** (86 товаров): без карт недоступен — тот же
  collector-вопрос.
- **Track 2 (map schema v2)** даст per-tool_type «Тип»/«Длина» — самый
  крупный unmapped-хвост (19+10 товаров по ROI-таблице).
Решение — за владельцем.

## Артефакты

27 файлов `07-*`: `/home/taximeter/proff58-staging/var/vi02/` + локально
`proff58/scratchpad/vi-int-02/` (scope-recompute, manifest+sha256,
dry-run, pack-a-contribution, failclosed-audit, image-preflight,
baseline-B4, recovery-plan, drift-gate, apply/postaudit ×2, rerun,
postaudit-final).
