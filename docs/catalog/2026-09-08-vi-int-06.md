# VI-INT-06 — реализация Pack A (ruleset expansion) → **PASS**

Дата: 2026-09-08. Продолжение `2026-09-07-vi-int-05.md` (RESEARCH_COMPLETE).
GO владельца: Track 1 / Pack A через PR → CI → merge → deploy staging →
read-only verification → STOP. **Batch B = NOT STARTED. Track 2 = NOT
STARTED.** Никаких staging enrichment writes, кроме scoped canonical
`load_attributes` (+2 утверждённые options).

## 1. Production rules source-of-truth

Доказано на deployed runtime: `catalog_import_scraped --category
vi-longtail` → `data_dir()` (`apps/catalog/ingest.py:26-28`) →
`/app/data/catalog_processing_rules/scraped_attr_map.vi-longtail.json`;
scope — `value_option__slug__in scope_tool_types` (command :80-83); select
resolution fail-closed (`scraped_import.py:624-633`: значение вне
`values` → dropped, option не создаётся никогда); source_priority —
`/app/data/attribute_rules.json`. Оба файла меняются только через PR →
CI → deploy. Canonical-механизм options — `attribute_rules.json` +
`load_attributes` (create-only, без молчаливых sort/flag-updates, ничего
не удаляет; полный `load_tool_types` для этой задачи не применялся).

## 2. Canonical AttributeOption audit

Модель: `value/slug/sort_order`, alias-полей нет; уникальность
`(attribute, value)` и `(attribute, slug)`. Поиск эквивалентов
crv/cr-v/хромованад/полипропилен по value и slug **всех осей** — 0 строк:
дублей нет, создание двух options легально. Артефакт `06-audit-options.json`.

## 3. Exact Pack A diff

- `scraped_attr_map.vi-longtail.json` (+55/−7): +«Квадрат»→drive
  (select, `{3/8 дюйма: d-3-8, 1/2 дюйма: d-1-2}`), +«Max усилие»→torque
  (decimal Н·м; **«Min усилие» НЕ маппится**), «Количество в
  наборе»→piece_count (decimal шт, замена unmapped-записи); material
  values += `crv`/`cr-v`→`crv-steel`, `полипропилен`→`polipropilen`
  (локально для material); scope_tool_types += ровно 10 (bp-motopompy,
  bp-nabory-pnevmoinstrumenta, elektronozhnitsy, machete-i-sekachi,
  miksery, nabory-burov, nabory-pilok, obor-shlangi, otboynye-molotki,
  zap-filtry). «Тип»/«Длина» не тронуты.
- `attribute_rules.json` (+18): 2 options (crv-steel «Хром-ванадиевая
  сталь», polipropilen «Полипропилен») в существующих material-блоках.

## 4. Implementation

Ветка `feature/vi-int-06-pack-a`: `adde5a2` (map+rules+tests+fixtures),
`263c1ac` (документация трека), `cfaaa2c` (DB-фикстура). Миграций нет
(`makemigrations --check` чист). ImagePipeline/taxonomy/schema v2/Track 2
не трогались.

## 5. Positive fixtures

`test_vi_pack_a.py` (20 тестов): 13002 — Квадрат→drive, Max
усилие→torque, CrV→crv-steel; 4667 — Cr-V→**та же** option; 36920/36921 —
полипропилен→polipropilen; scope-видимость новых типов (miksery виден,
вне-scope — нет); запись crv-steel (DB-backed).

## 6. Negative fixtures (fail-closed)

«Длина=100» моток ≠ length · «Длина=50 м» ≠ global length ·
«Тип=щелчковый» ≠ bit_type · generic «Тип» не импортируется · «Тип
инструмента» ≠ PAV (taxonomy write отсутствует) · «Объем=5.8 кг» ≠
volume · «Материал=металл»/неизвестный material → dropped без auto-create
· «Min усилие» ≠ torque.

## 7. Batch A v2 regression

Replay 20 карточек Batch A: 23 применённых значения извлекаются
неизменно; higher-priority mutation 0; semantic regression 0; schema
expansion вне разрешённого 0. Дельта extraction — ровно 11
задокументированных Pack A-доборов на этих же картах
(12957/12959/22672/22674/36919) — заявленный прирост, не неожиданность.

## 8. Offline replay CURRENT vs PACK A — MATCH 1:1

Production-логика ветки, 66 collector-карт (`06-replay-gate.json`):

| метрика | CURRENT | PACK A | Δ |
|---|---:|---:|---:|
| map-covered remaining | 16 | 36 | +20 |
| strict collected covered | 4 | 15 | +11 |
| estimated safe PAV | 4 | 24 | +20 |
| meaningfully enriched | 3 | 14 | +11 |

## 9. DB-backed dry-run (свежий staging-derived sandbox)

- `migrate` → «No migrations to apply» (миграций в ветке нет);
- importer dry-run на 66 картах: matched 60/66, ambiguous 0, conflicts 0;
  6 not_found — в точности 5 отклонённых simulation 0-safe-PAV типов;
  create 38 (material 12, weight_kg 11, piece_count 5, torque 3, drive 3,
  …), skipped_voltage 6, dropped fail-closed (options не создаются);
  per-product plan == simulation 1:1; higher-priority PAV protected;
  Attribute/CategoryAttribute/taxonomy delta 0; DB drift после dry-run 0;
- `load_attributes --dry-run`: Pack A-дельта ровно +2 options; в плане
  также pre-existing дрейф dev↔staging (1 attribute update + 8 bindings —
  присутствует и без Pack A, доказано контрольным прогоном с rules из
  origin/dev) — **на staging не применялся** (scoped-режим);
- pytest против sandbox-Postgres: 20/20 (после фикса DB-фикстуры `cfaaa2c`).

## 10. Tests

`test_vi_pack_a.py` 20/20 (sandbox); локально DB-free 19/19 зелёные
(DB-backed — ENVIRONMENT BLOCKED локально, выполнен на sandbox);
ruff/black clean (пины 0.4.8/24.4.2); полный каталоговый suite — в CI.

## 11. Migration/schema check

`makemigrations --check --dry-run` — «No changes detected» на всех
этапах; миграций в PR нет; options — через canonical loader, не миграция.

## 12. Git hygiene

`git status` чист; diff к origin/dev: 9 файлов, +2490/−7 (map, rules,
тесты+2 fixture, 4 документа); дампов/scratchpad/collector
artifacts/credentials/Track 2/ImagePipeline-правок/unrelated changes —
нет.

## 13. Commits

`adde5a2` feat(catalog): VI-INT-06 Pack A — расширение vi-longtail map
(3 поля, 2 options, 10 scope) · `263c1ac` docs (отчёты VI-INT-03/04A/05 +
промпт VI-INT-05) · `cfaaa2c` test: DB-фикстура 16 осей.

## 14. PR

**#704** → dev: G-3, результаты VI-INT-05, 3 mappings, material
normalization (CrV/Cr-V — одна сущность), 2 options, 10 scope, +20 safe
PAV / +11 enriched, positive/negative fixtures, отсутствие global
«Тип»/«Длина»/schema v2/staging writes; scoped-подход к
pre-existing дрейфу load_attributes зафиксирован явно. Batch B не включён.

## 15. CI

4/4 green: lint 16 с, frontend 1m0s, test 4m37s, catalog-rules-gate 19 с
(run 34151608393). Повторных заходов не потребовалось.

## 16. Merge SHA

`f1672ab6c559434e06179b9d07a9aa877c3b04af` (2026-09-07 18:31 UTC).

## 17. Deployed SHA

`f1672ab6` (deploy run 34152007469 — **completed/success**, без
SSH-таймаута: .dockerignore из PR #684 снял проблему 940 МБ context;
контейнеры пересозданы, web healthy).

## 18. Post-deploy read-only verification — PASS

- deployed SHA == merge; 8 контейнеров Up (web/db/frontend healthy);
  `showmigrations` — 0040, новых нет; `makemigrations --check` чист;
- production runtime map: scope = 40; 3 mappings активны; «Min усилие»
  ≠ torque; global «Тип»/«Длина» отсутствуют;
- **scoped `load_attributes`** (минимальный rules-файл, `--path`):
  план дословно «create 2 options, 0 прочего» → apply: material 20→22,
  ровно crv-steel+polipropilen, изменённых/удалённых 0; pre-existing
  дрейф dev↔staging **не применялся**; Attribute 100=100,
  CategoryAttribute 244=244, **PAV 68181=68181, ProductImage 696=696**;
- runtime extract: CrV → crv-steel и Cr-V → crv-steel (одна option),
  полипропилен → polipropilen, 3/8 дюйма → d-3-8;
- options ровно ×1 каждая; Attribute/Option delta == утверждённому scope;
  source_dist изображений без изменений; taxonomy/tool_type данных не
  изменилась.
- Замечание: новые options созданы с sort_order=0 (scoped-файл) —
  косметика порядка фасета, не дефект; rollback-команда (delete 2 строки
  по `(attribute, slug)`) подготовлена, не понадобилась.

## 19. Exact changed files

`data/catalog_processing_rules/scraped_attr_map.vi-longtail.json` ·
`data/attribute_rules.json` · `apps/catalog/tests/test_vi_pack_a.py` ·
`apps/catalog/tests/fixtures/vi_pack_a_batch_a_export_v2.json` ·
`apps/catalog/tests/fixtures/vi_pack_a_chars_dryrun_v2.json` ·
`docs/catalog/2026-09-05-vi-int-03.md` ·
`docs/catalog/2026-09-07-vi-int-04a.md` ·
`docs/catalog/2026-09-07-vi-int-05.md` ·
`docs/VI-INT-05_ruleset_expansion_research_prompt.md`.

## 20. Новые GAP

Нет. Известные отложенные: pre-existing дрейф `load_attributes`
dev↔staging (1 attribute update + 8 bindings) — не VI-INT-06 scope,
виден в плане loader'а, зафиксирован (`06-load-attributes-plan*.json`);
Track 2 список — в VI-INT-05.

## 21. Batch B = NOT STARTED

## 22. Track 2 = NOT STARTED

Map schema v2, per-tool_type «Тип»/«Длина», targeted collector pool 16,
«Трещотка»/«Класс товара»/«Цвет»/«Min усилие», unit research — всё
отложено, отдельный OWNER GO.

## Итог

**VI-INT-06: PASS.** Pack A в production: карта расширена (3 mappings +
10 scope через deploy), 2 canonical options добавлены scoped-load'ом,
replay совпал с research 1:1, данных каталога не изменено, дрейф не
тронут. Следующий шаг — OWNER-решение о Batch B на расширенном ruleset.

## Артефакты

17 файлов `06-*`: `/home/taximeter/vi02/` + локально
`proff58/scratchpad/vi-int-02/` (audits, pack-extract, replay-gate,
sandbox dry-run, load plan/post, verification, pytest xml, pr-body).
