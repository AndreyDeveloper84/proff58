# VI-INT-11: PLANNING_COMPLETE

Collector Wave 2 Planning — финальный отчёт. Всё read-only: collector/source не
трогались, staging writes — нет, code/map/schema — нет, git — нет.
Артефакты: `/home/taximeter/vi02/11-*` + `scratchpad/vi-int-02/11-*`.

## 1. Цель

Спланировать Collector Wave 2 по фактическим данным 4 runs (pilot-30, batch-a run-1,
batch-a2, research-16) и актуальному long-tail — без запуска сбора.

## 2. Actual recount (п.1)

Predicate на сегодня (tail tool_type 1–5 в пуле + identity A + без фото):
**remaining = 70** (pool 6148, tail types 117) — drift vs VI-INT-09 отсутствует.
По 70: без изображений 70; с характеристиками (>1 PAV) 6; со strict-картами 7
(22673 + 6 unused старых); SKM 13; CONFLICT 0 (2620/2624 выбыли из long-tail —
дрейф зафиксирован); UNRESOLVABLE 1 (42072); in deployed scope 13; locked 0.
`11-recount.json`.

## 3. Candidate universe (п.2)

**63** товара (70 − 7 с собранными strict-картами): все active, не locked,
с артикулами, без чужих активных треков. `11-universe.json`.

## 4. Identity difficulty (п.3)

**I1: 17 · I2: 0 · I3: 39 · I4: 14** (13 исторических SKM + 42072).
I3 (generic numeric) — доминирующий класс риска. Правила — `11-identity-classes.json`.

## 5. Historical failures (п.4)

18 записей (`11-failures.json`): pilot-30 (4 SKM + 1 CONFLICT), run-1 (5 SKM),
run-2 (4 SKM + 2 повтора + 1 CONFLICT), research-16 (2 SKM + 1 UNRESOLVABLE).
**retry_justified=False у всех** (повторы 11093/42055 доказали неизменность).

## 6. Current-map characteristics ROI (п.5)

По типам — только deployed 16 mapped полей; unmapped ≠ yield. Ненулевой исторический
safe-PAV/card только у типов с картами weight/power/width/material; большинство
remaining-типов — 0 (карточек нет). `11-char-roi.json/.md`.

## 7. Image ROI (п.6)

По историческим runs: типы-инструменты давали 3 refs/card; strict rate по типам
с картами — 1.0 почти везде. Прогноз ≠ гарантия. `11-image-roi.json/.md`.

## 8. Combined ROI score (п.7)

`score = (I1I2 × strict_rate) × (1 + 0.5×expPAV + 0.3×refs) / (remaining × 3.6)`.
Топ: aksessuary-sharnirno 0.528 (карта на диске), boltorezy 0.352, shtoborezy/
stanki-derevoobrabatyvayushchie/stameski/drovokoly/sverlilnye/ustanovki 0.139.
`11-combined-roi.json/.md`.

## 9. Top-ROI types пересчёт (п.8)

boltorezy — НЕ победитель заранее (0.352, 3 remaining, 2 с картами); izm-dalnomery/
siz-nakolenniki/kobury — 0.0; shtoborezy/stanki — 0.139 без карт (пробник-класс).

## 10. izm-dalnomery правило (п.9)

54/54 unmapped (replay VI-INT-08) → **HIGH IMAGE ROI / LOW CHAR ROI**; ranking
не повышался; future_v2_potential в score не включён.

## 11. Schema v2 / Scenario C (п.10–11)

В current ROI не включены; v2 — DEFER (08-schema-v2-roi.md: ~9 PAV remaining).

## 12. Request-cost model (п.12)

Фактические: pilot-30 3.2, batch-a2 3.3, research-16 5.6 (новые типы + 42072 burn).
По классам: I1 2.5–3.5, I2 3–5, I3 5–8 (риск SKM), I4 12–16+ (unresolvable).
Диапазон на товар 2.5–5.5 loads; SKM 2–4; captcha ~1/90 → STOP по контракту.
`11-cost-model.json`.

## 13. Wave 2A — High confidence/High ROI (13 товаров)

Состав (`11-wave-2a-manifest.json`, все I1):
- **31957** boltorezy ЗУБР 23311-075_z01 — карты семейства доказали weight/length;
- **44908, 44910, 44911** stanki-derevoobrabatyvayushchie (ЗПДС-200-800, РС-305, РС-320);
- **45480, 45483, 45486, 45488** shtoborezy (ЗШ-П30/45/65);
- **33229** stameski (18097-H5); **43994** drovokoly (WSE15374);
- **44928** sverlilnye-stanki (ЗСС-350); **44985** ustanovki-almaznogo-bureniya (HBD395);
- **24740** payalniki (55405-40_z01).

Разнообразие по типам (8 типов), boltorezy получил 1 слот (ROI-доказательство,
не representation), без I3/I4. Expected: requests 33–46 loads ≈ 4–5 сессий;
strict 11–13/13; image-capable 10–13; current-map PAV 0–13 (факт = dry-run
после сбора). Это PROPOSED, не frozen: перед запуском — отдельный freeze
по протоколу VI-INT-08 (revalidation → immutable manifest + SHA256).

## 14. Wave 2B / 2C

- **2B (1 тип):** gravery/steplery-класс — I1-кандидаты при нулевом type-score;
  запуск только после факта 2A.
- **2C (30 типов):** I3 generic numeric, исторические SKM, conflict-prone,
  izm-dalnomery (unmapped), I4 — может не запускаться вообще.

## 15. Stopping rules + checkpoint (контракт будущего run'а, дословно)

- CAPTCHA → STOP/cooldown по существующему контракту (прогрев человеком, ≥2 ч).
- Session budget → STOP/resume (planовая ротация 9000 с).
- SESSION_BUDGET_STUCK → UNRESOLVABLE_IN_BUDGET (класс доказан на 42072 —
  товар фиксируется, wave продолжается без него).
- Unexpected source layout change → STOP.
- Identity false-positive signal → STOP.
- Systematic search degradation → STOP.
- Request cost выше planning (>5.5 loads/prod sustained) → checkpoint.
- Strict rate ниже ожидаемой (<8/13 после 2/3 wave) → checkpoint.
- **Checkpoint после первых 5 успешно обработанных кандидатов 2A:** strict rate,
  loads/product, image refs, characteristics potential, source health; резкое
  ухудшение экономики → остаток wave не продолжать автоматически.

## 16. Downstream write protocol (п.19)

collector → research audit → immutable write manifest → G-4 baseline → backup →
apply → rerun → no-op → **OWNER decision на write отдельно** (не автоматически
после сбора). Зафиксировано как контракт.

## 17. Decision table (п.22)

39 remaining tool_type, волны: **2A = 8 типов, 2B = 1, 2C = 30**.
`11-decision-table.json/.md` — каждая строка объясняет wave.

## 18. Recommendation (п.23)

**R1 — GO WAVE 2A.** Обоснование цифрами: research-16 дал 13/16 strict (81 %) на
смешанном пуле; Wave 2A — I1-pure (ожидание выше); источник стабилен (1 капча/90
loads, 0 в research-16); cost 33–46 loads ≈ 4–5 сессий ≈ 1–2 захода; image-first
value доказана (13/13 refs в research-16). R2/R3/R4 отклонены: probe-first не
нужен (I1-доказательство уже есть), schema-research не блокирует сбор изображений,
pause лишает бесплатного image yield.

## 19. Что НЕ делать

Collector массовый (63 universe) — NO; retry I4 — NO; schema v2 — defer;
новые атрибуты — research later; characteristics для типов с доказанным 0
safe PAV (izm-dalnomery) — NO.

## 20. Риски

I3-класс (39 товаров) — основной источник дорогих сборов; источник меняет выдачу
без предупреждения; окно нагрузки хоста плывёт (доказано 09.09: wa 44–94 % днём).

## 21. Связь с VI-треком

VI-INT-03 (25 fixture) → 04A (Batch A 20) → 05/06 (Pack A ruleset) → 07 (Batch B
15) → 08 (research 16) → 09 (combined 13) → **11 (план Wave 2)**. Каждый шаг —
с backup/baseline/provenance/rerun/no-op.

## 22. Состояние long-tail

remaining 70 (101 → 70 за трек: Batch B 15 + VI-INT-09 13 + foreign 3 по фото);
strict-carded неиспользованных в scope — только 22673 (без refs).

## 23. Артефакты

`11-*` (11 файлов): recount, universe, identity-classes, failures, char-roi(.md),
image-roi(.md), combined-roi(.md), cost-model, wave-2a-manifest, decision-table(.md),
report.md.

## 24. Отклонения/оговорки

- I2-класс пуст (0) — правила не нашли смешанных артикулов вне истории промахов.
- score типов без карт — по I1I2×0.5 (strict_rate None → 0.5) — помечено.
- report.json collector'а перезаписывается между заходами (TECH_DEBT) — cost
  считан по логам (grep browser get), не по последнему сегменту.

## 25. Вердикт

**VI-INT-11: PLANNING_COMPLETE.** Wave 2A (13 I1-товаров, 8 типов) готова к
freeze → collector по GO владельца; decision table покрывает все 39 remaining
типов; stopping rules и downstream write protocol зафиксированы. Ничего не
запускалось и не применялось.

После отчёта — стоп.
