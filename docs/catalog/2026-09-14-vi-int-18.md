# VI-INT-18 — Old Strict Image Cleanup (6 strict / 18 refs) → **PASS**

Дата: 2026-09-14. Продолжение `2026-09-10-vi-int-16.md` / VI-INT-17
(docs closeout). Image-only controlled apply на существующем evidence
(6 old strict cards, 18 refs). Трек содержал инфра-блокировку
(BLOCKED_INFRASTRUCTURE → INFRA-DISK-01) и был возобновлён с fresh drift
gate по контракту — не FAIL.

## Вердикт: PASS / CLOSED

- 18/18 exact manifest refs materialized (ids 870–887) · +18 exact
  attributed ProductImage · 3 images/product · 1 main/product · provenance
  complete · duplicates 0 · orphan/missing files 0 · **PAV zero-write** ·
  Product semantic zero-write · schema/taxonomy/tool_type zero-write ·
  G-4: global +18 == exact ours +18, foreign 0 · rerun = полный no-op
  (downloads 0) · replacements 0 · unrelated writes 0.

## Этап 1 (read-only) — ключевые гейты

1. **Revalidation:** 6/6 clean, drift 0; strict identity по цепочке
   article_exact+brand_exact у всех (ни model-only/fuzzy/alias).
2. **Аудит 26863 → PASS:** needs_review — июльский маркер выбора
   tool_type (research-run 18.07, «типа нет в словаре»), закрыт 19.07
   назначением `krep-shplinty`; к identity/images отношения нет.
3. **Source card immutability:** sha256 всех 6 карточек зафиксированы;
   карты не редактировались/не дополнялись.
4. **Refs inventory:** факт **18 == forecast 18** (3×6), без подгонки.
5. **URL preflight:** 18/18 https/443, контракт M-13, дублей 0 (внутри
   product и против всей БД).
6. **Current ProductImage audit:** existing 0, main 0 у всех 6.
7. **Immutable manifest:** `18-image-manifest.json`, **SHA256
   `8bfc95e5337a2e23676c4b4f2e74987e418040270c4ca91745c5e5e8ddc20990`**;
   18 CREATE, 1 main candidate/товар (первый по position).
8. **Batch gate:** products_checked 6, writeable 6, CREATE 18, DEDUP 0,
   REJECT 0, expected_downloads 18, expected_delta 18.

## Инфра-инцидент (между этапами)

- **STOP до первого write:** диск `/` = 100 % (737 МБ), хост клинчился на
  `du`/`docker system df`; гейт ≥2 ГБ не пройден. Принято владельцем как
  **BLOCKED_INFRASTRUCTURE / WRITE NOT STARTED** (не FAIL): manifest gate
  CLEAN, baseline без drift, backup создан, DB/media writes = 0.
- **INFRA-DISK-01 (read-only audit):** главный потребитель — Docker build
  cache **13,9 ГБ (0 active, reclaimable)** + 2 dangling-образа ~3,2 ГБ;
  volumes классифицированы, не тронуты; safety-dump сохранён.
- **Cleanup по OWNER GO (шаги 1–2):** `docker builder prune -f` +
  `docker image prune -f` → **734 МБ → 11 ГБ free**; stability 0 дрейфа
  (5×2 мин); 19 контейнеров healthy; новый operational gate ≥5 ГБ — PASS.
  Артефакты: `infra-disk-01-audit.json/.md`, `infra-disk-01-cleanup.json`.

## Этап 2 (controlled apply)

### Fresh drift gate (п.25 INFRA-DISK-01) — PASS, drift = 0

manifest SHA == `8bfc95e5…2090`; exact 6 products (identity прежняя,
content_locked false); галереи пусты; refs conflict-free; global ==
baseline (ProductImage 787, PAV 84839, Attribute 110, Option 675,
CategoryAttribute 298, migrations 128); backup `db-20260914-102042-pre-
vi-int18.dump` — sha256 `bab8b959…` совпал, TOC валиден (TABLE DATA 72).

### Pilot 31935 — PASS

created = 3 (ids 870–872); main = 1 (position 1, == expected_is_main);
duplicates 0; provenance полный; файлы на месте; ordering == manifest;
Product/PAV/tool_type/category/attrs_cache unchanged; unrelated delta = 0.
Recovery-plan (`18-recovery-plan.md`) написан до write: rollback только
по exact IDs + provenance-предикату.

### Full apply — PASS

31938, 26863, 28319, 33252, 42549: refs 15 → created 15, dedup 0,
rejected 0. По каждому: images=3, mains=1. Global 787 → **805**.

### Audits (52/52 проверок) — PASS

- **Main invariant:** main_count == 1 у всех 6, main == expected;
- **Duplicates:** dup url/checksum/rows = 0; cross-product checksum
  incidents = 0;
- **Provenance:** 18/18 (source=vseinstrumenti, source_url, checksum
  64hex == sha256 файла, fetched_at);
- **Files:** 18/18 exists, size>0, webp;
- **G-4 attribution:** global delta **+18 == exact ours 18** (по
  provenance IDs) + **foreign 0**; source dist: vseinstrumenti 212→230,
  прочие источники без изменений;
- **PAV zero-write:** 84839→84839, scope unchanged;
- **Product zero-semantic-write:** category_id/tool_type/active/
  content_locked/article/brand/attrs_cache unchanged у всех 6;
- **Schema zero-write:** Attribute 110, Option 675, CategoryAttribute 298
  (sha совпали), migrations 128; taxonomy unchanged;
- **Public read:** API `/api/catalog/products/<slug>/` 6/6 HTTP 200, 3
  изображения, 1 main, битых URL 0 (первая попытка 400/301 — артефакт
  тестового клиента: DisallowedHost/SECURE_SSL_REDIRECT; с корректным
  Host+https — PASS; routing-issues нет).

### Rerun — PASS

CREATE = 0 · **network downloads = 0** (все 18 dedup до сети —
подтверждено runtime-счётчиком `_download`) · existing/keep = 18 ·
global 805→805 · PAV delta 0 · main/checksum/provenance unchanged.

### Final

- **26863:** needs_review маркер существует и не мутирован, tool_type
  `krep-shplinty` unchanged;
- **Download accounting:** expected 18 / successful 18 / retried 0 /
  failed 0 / HTTP 200 ×18 / **replacements = 0** / rejected_refs = [];
- **Per-product:** 31935 [870,871,872] · 31938 [873,874,875] · 26863
  [876,877,878] · 28319 [879,880,881] · 33252 [882,883,884] · 42549
  [885,886,887] — у всех refs matched 3/3, broken 0;
- **Long-tail recount (по предикату):** **64 → 58** — выбыли ровно 6
  old-strict (получили галереи 3×6), причина у каждого: created 3 refs
  по manifest.

## Примечания

- Backup `db-20260914-102042-pre-vi-int18.dump` — MUST_KEEP, на месте.
- Code/config delta = 0; source requests = 0 (только downloads frozen
  refs); git delta = 0; нумерация шины скорректирована (vi-track-0012 =
  max+1 после ERROR-0011).
- Откат не потребовался; recovery artifacts на месте.

## Остаток / recommendation

- **Strict image inventory исчерпан окончательно** (все 12 unused strict
  обработаны: 6 Wave 2A + 6 old strict; 22673 без refs).
- **Package B/C/D (S2=4, S3=7, S4=9)** — research inventory, NOT STARTED.
- **Collector:** следующий шаг требует нового сбора — candidates: Wave 2B
  (43824 gravery + присоединение типов с высоким image ROI из
  11-decision-table), Wave 2C пересмотр (image-first слой без
  shtoborezy-класса).
- **Docs:** VI-INT-18 отчёт + этот цикл — кандидат на следующий docs
  closeout PR вместе с INFRA-DISK-01 запиской.

## Артефакты

`18-*` (30+ файлов): `/home/taximeter/proff58-staging/var/vi02/` +
локально `proff58/scratchpad/vi-int-02/` (revalidation, audit-26863,
source-cards, refs-inventory, url-preflight, ordering-plan,
image-manifest+sha256, baselines, recovery-plan, fresh drift gate,
pilot-attribution, images-apply-1, postaudit-images, rerun,
postaudit-final, runner-скрипты) + `infra-disk-01-*`.
