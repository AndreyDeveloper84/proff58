# VI-INT-12: COLLECTION_COMPLETE

Collector Wave 2A Execution — финальный отчёт. Всё read-only: staging writes — нет,
media writes — нет, code/collector/map/schema — нет, git — нет, капча не обходилась.
Артефакты: `/home/taximeter/vi02/12-*` + `scratchpad/vi-int-02/12-*`.

## 1. Revalidation (п.1–2)

10/13 clean (`12-revalidation.json`): active+stock, article/brand не изменились,
tool_type совпадает, images 0, content_locked False, strict card не появилась,
чужих треков нет. **3 SCOPE_DRIFT** (44910, 44911 stanki-derevoobrabatyvayushchie,
43994 drovokoly) — незакрытые `needs_review` из research-run **2026-07-31**
(run completed; исторические маркеры июльской модерации tool_type). По протоколу
исключены из freeze, замен не искалось (13 — не квота).

## 2. Drift после freeze

Zero (`12-image-audit.json`: staging images drift = 0 по всем 6 strict за время
сбора; pre-write drift-проверки в треке не требовались — writes не было).

## 3. Immutable collector manifest + SHA256

`12-wave2a-manifest.json` (10 товаров), sha256 `79527fc91ec2d7cdc1052815dd1621267520eb46f1fb56ebc96fb535285cd368`;
input `12-wave2a-input.json` sha256 `099dbd3e1899a20636d8e8232d2831131e16d5f874c7328cf883367abc2a412f`.
Оба сверены перед стартом.

## 4. Execution sessions

Run-01: старт 2026-09-09 19:32:09 МСК (tmux viwave2a), штатное завершение
2026-09-10 00:54 МСК. 3 сессии (loads 9/9/9), **2 плановые ротации** (9000 с),
cooldowns доминируют хронологию.

## 5. Checkpoint first 5 + verdict

**Процессное отклонение (зафиксировано как есть):** checkpoint-пауза физически
НЕ исполнена по таймингу — 5-й outcome (44928, 22:11:07) и 6-й (44985, 22:12:57)
прошли внутри одной свежей сессии за ~4,5 минуты без естественной точки остановки.
**Gate оценён ретроактивно: PASS** — первые 5 outcomes: strict **5/5**
(все article_exact+brand_exact), cost **2,2 loads/product** (≤ 5,5),
captcha 0, traceback 0, layout-аномалий нет, false-positive нет.

## 6. Remaining execution

После checkpoint-точки: 45480 → SKM (22:15), ротация №2 (22:16, cooldown до ~00:47),
остаток 45483/45486/45488 → все SKM, штатное завершение 00:54.
SESSION_BUDGET_STUCK / CAPTCHA STOP не было.

## 7. Request accounting (инвариант соблюдён)

total browser_loads **27**, by_session **[9, 9, 9]**, sum(by_session) == total ✓;
search_loads 21, cards_opened 6, rotations 2, captcha 0.
loads/product **2,7**; loads/strict 4,5; SKM стоит ~9 loads (3 queries × (1+2)).

## 8. CAPTCHA incidents

**0** за весь execution (ни одной эскалации).

## 9. Identity outcomes

**6 MATCHED_STRICT / 4 SEARCH_KEY_MISS**; conflict 0, ambiguous 0,
unresolvable 0, blocked 0. `12-identity-audit.json`.

## 10. Strict evidence audit

У всех 6: reason=`article_exact+brand_exact` (проверено поштучно), article/brand
evidence совпадает с input; **false assignment = 0**, сомнений нет. Все 6 — по
1 поисковому запросу (дёшево). SKM ×4 — один тип (shtoborezy), по 3 queries —
класс артикулов ЗШ-П источником не покрыт.

## 11. Source-card completeness

matches.json: 6 записей, полные карточки (chars 3–31, image_refs 1–3);
search-key-miss.json: 4 записи с queries_tried; потерянных outcomes = 0.

## 12. Existing-map replay

6 strict-карточек через production `extract_card_values` на deployed staging
(read-only): raw extraction — `12-existing-map-replay.json`.

## 13. Raw-map potential

**15 PAV** (24740: 3, 33229: 1, 44908: 5, 44928: 4, 44985: 2, 31957: 0) —
извлекаемо текущей картой, БЕЗ учёта scope. Только информационно.

## 14. Deployable CURRENT yield

**0** — все 6 типов (payalniki, boltorezy, stameski, stanki-derevoobrabatyvayushchie,
sverlilnye-stanki, ustanovki-almaznogo-bureniya) вне deployed scope (40).
OWNER опирается только на это число: характеристики сейчас не деплоятся,
Wave 2A — image-first по структуре.

## 15. Fail-closed audit

`12-failclosed-audit.json` — FAIL_CLOSED_OK: «Тип»/«Тип инструмента»/«Длина»/
«Min усилие»/«Объем» не импортируются; dropped_select ×1 («Питание=от сети» —
fail closed); новые unit-поля волны (Мощность (Вт) ×2, Плавный пуск ×2,
Регулировка оборотов ×2, Материал рукояти/обработки) → UNMAPPED/research evidence,
mapping не добавлялся.

## 16. Image-ref audit

**16 refs** по 6 strict (min 1 / median 3 / max 3), дубликатов 0, невалидных
по M-13 = 0, products without refs = 0, staging drift после freeze = 0.
31957 boltorezy — единственный с 1 ref (у источника одно фото).

## 17. Forecast vs actual

| метрика | forecast (по 13 proposed) | actual (10 после SCOPE_DRIFT) |
|---|---|---|
| candidates | 13 | 10 (−3 needs_review) |
| strict | 11–13 (84–100 %) | 6 (60 %) |
| loads | 33–46 | 27 |
| loads/product | 2.5–3.5 | 2.7 |

Variance: strict ниже — все 4 SKM одним типом (shtoborezy, класс ЗШ-П не покрыт
источником); research outcome, не defect. Loads — в норме/ниже forecast.

## 18. Actual cost model update

I1 с брендовым артикулом: ~2–3 loads/product (6 товаров по 1 query).
SKM-класс: ~9 loads на товар (3 пути поиска × (1 search + 2 карточки)).
Без shtoborezy: 6/6 = 100 % strict на I1. Поправка внесена в `12-wave2bc-update.json`.

## 19. Meaningful enrichment potential

Images: **6 товаров / 16 refs** — все выбывают из long-tail после apply.
Chars deployable: 0 (scope). Raw potential: 15 PAV — требует расширения scope
(отдельное решение владельца).

## 20. Write packs A/B/C/D

`12-write-packs.json`: **все 6 — класс B IMAGE_ONLY** (deployable char ops = 0,
refs по позиции, existing staging images 0). A/C/D — пусты. Ничего не применять.

## 21. Old strict separately

6 старых unused strict (31935, 31938, 26863, 28319, 33252, 42549) + 22673
(in-scope strict без refs) — отдельной секцией, **не смешивать с Wave 2A без
OWNER решения**.

## 22. Wave 2B/2C evidence update

`12-wave2bc-update.json`: gravery 43824 (DREMEL) — может быть +1 в следующий
combined track (не отдельный wave); 2C без изменений; новых failure classes нет;
strict expectation скорректирован с поправкой на тип-концентрацию промахов.

## 23. New GAP

shtoborezy (ЗШ-П класс, 4 товара из remaining) — источник не покрывает артикулы;
оставшиеся 3 (45483/45486/45488) — SKM, добавлены в failures ledger
(`11-failures.json` дополняется при следующем planning-цикле).

## 24. report.json TECH_DEBT

Известная перезапись report.json между заходами — **копия сохранена вручную**:
`collector/wave-2a-run-01-report.json` (копии в `/home/taximeter/vi02/` и локально).
Итоговый report (stopped_reason=None, counts 6/4) соответствует логу.

## 25. Staging DB delta = 0

Ни одной записи за весь этап (guard SELECT-only; drift-проверки подтверждают).

## 26. Staging media delta = 0

Probe-режим — ничего не скачивалось.

## 27. Code/map/schema delta = 0

Collector contract, карта, схема — не менялись.

## 28. Вердикт по критериям

- immutable manifest соблюдён (sha256 сверен, состав не менялся);
- checkpoint: gate PASS (ретроактивно 5/5, 2.2 loads/product), пауза не исполнена
  по таймингу — процессное отклонение зафиксировано, не скрыто;
- false assignment 0; staging untouched; контракт collector'а соблюдён;
- исходы 6 strict + 4 SKM учтены без потерь.
⇒ **VI-INT-12: COLLECTION_COMPLETE.**

## 29. Recommendation

**Owner GO на image-apply для 6 strict (16 refs)** по протоколу VI-INT-09
(backup/baseline/drift-gate/provenance/rerun): весь deployable yield волны —
изображения. Raw-map potential 15 PAV — НЕ вводить без отдельного решения о
scope. Следующий combined track: gravery 43824 (+1) как кандидат; Wave 2B/2C —
не запускать; shtoborezy — в failures ledger. После отчёта — стоп.
