# VI-INT-21: PASS / NO_APPLY

Micro Image Recovery — итоговый отчёт. Phase A (22673 re-extract) + Phase B
(43824 strict collector) + Phase C (decision gate). Оба execution корректны,
refs не найдено ни для одного продукта — это НЕ FAIL: failure не execution и
не safety характера, а фактура источника.

Запреты соблюдены: ProductImage write 0 · staging write 0 · code/config delta 0
· git 0 · scope expansion 0 · captcha/stealth bypass 0.

Артефакты: `/home/taximeter/vi02/21-*` + локально `scratchpad/vi-int-02/21-*`
+ docs-копия (незакоммичено, будущий closeout VI-INT-20+21 одним PR по
решению владельца).

## 1. Environment

- Deployed SHA (host checkout `/home/taximeter/proff58-staging`):
  `8a51fbf3fd9406395f05eeea570c8b2586f6a19a` (без изменений с VI-INT-20).
- Disk: 11G free / 87% (гейт ≥5G соблюдён с запасом).
- Staging guard: `current_database()=='proff58_staging'` во всех скриптах.

## 2. Baselines

Из VI-INT-20 (14.09): Product 47225 · ProductImage 805 · PAV 84839 ·
Attribute 110 · Option 675 · CategoryAttribute 298 · migrations 128 ·
remaining long-tail 58. Phase C recount подтвердил всё без дрейфа (см. п.10).

## 3. 22673 — Phase A

- **A0 preflight** (`21-a-preflight.json`): CLEAN, drift 0 — active, stock 3,
  article 76430, brand СЕРВИС КЛЮЧ, tool_type nabory-klyuchey-imbusovyh,
  images 0, content_locked False, foreign image activity нет.
- **Old card verification**: strict card с batch-a run-2 (2026-09-07),
  article_exact+brand_exact, vseinstrumenti 1185279.
- **A1 offline extraction** (`21-a-offline-extract.json`): НЕВОЗМОЖНА —
  collector probe-режим не персистит raw HTML (проверены out-live/*, var/*,
  find по 1185279/22673 — 0). Extractor не менялся с run-2 (repo last commit
  2026-03-17) — «исправленной логики» нет; sibling 22674 в ту же ночь получил
  3 refs.
- **A3 network**: ровно **1** bounded fetch известной карточки (без
  search/expansion/model/alias), существующий VIBrowserSession + profile-run.
  Pre-flight: первое окно START_FAIL (updatedb.mlocate 40+ мин в D-state,
  io pressure ~90, wa 48–90%) — дождались чистого окна (wa ~13%, load 1.08).
  Raw artifact immutable: `var/card-html/22673-1185279.html`, sha256
  `84c36ea61d68cee4ba2f5230cda7b713a730dc30c2dcfa6a4e3116cc848ff8c9`,
  174792 chars, chmod 444.
- **A4 fresh identity**: article 76430 exact ✓ brand СЕРВИС КЛЮЧ exact ✓
  canonical URL unchanged ✓ JSON-LD Product+Offer(348, InStock) — жива,
  не superseded.
- **A5 refs**: 0 (card path и DOM control — оба 0). Root cause:
  `Product.image = ["…/res/content/good/noPhoto.png"]` — **у карточки
  источника нет фотографий** (плейсхолдер), CDN-ссылок с goods-id 1185279
  в HTML ноль.
- **A6 final class**: `RESEARCHED_NO_REFS / EXHAUSTED_CURRENT_SOURCE`.
  Без новой source strategy не retry.

## 4. 43824 — Phase B

- **B1 preflight** (`21-b-preflight.json`): CLEAN, drift 0 — active, stock 1,
  article DRM-F0130290JZ, brand DREMEL, tool_type gravery, images 0,
  content_locked False, processing history пусто.
- **B2 execution**: окно чистое (wa 0–5%, io avg10 4.4, swap stable, mlocate
  завершён); профиль без stale Singleton; smoke SESSION_OK
  (START 24.0 / NAV 31.8 clean / CLOSE 5.0 — лучше ориентиров run-08).
  Input `21-b-43824-input.json` sha256 `bc5b7248…b6`. Run: существующий CLI,
  probe, те же флаги; 1 сессия, **3 search loads**, 0 ротаций, captcha 0,
  ~100 с.
- **Search accounting**: 3 контрактных запроса (article / article+brand /
  name), 15 кандидатов суммарно, **15/15 отклонены exact-фильтром артикула**,
  карточки не открывались (cards_opened 0), queries не расширялись.
- **B3 identity**: не понадобился (нет карточки). **B6 conflict**: нет.
  **B7**: blocking class = historical SKM — не обходили.
- **B8 refs**: нет; raw source card artifact отсутствует (карточки не
  открывались), sha256 n/a.
- **B9 final class**: `SOURCE_BLOCKED` (источник не покрывает артикул
  DRM-F0130290JZ в выдаче; вероятно живёт под артикулом без префикса DRM-,
  но expansion вне контракта B4 не выполнялось).

## 5. Incidents

0 execution/safety инцидентов. 2 транспортных сбоя запуска Chrome в Phase A
(stale SingletonLock с 10.09 + IO-pressure окно) — ни одной загрузки
источника в них не было; оба закрыты дисциплиной (снятие локов, ожидание
окна). Капч/челленджей 0.

## 6. Decision gate table (21-c-decision-gate.json/.md)

| Product | Identity | Search requests | Card fetches | Refs | Result |
|---|---|---|---|---|---|
| 22673 | strict known | 0 | 1 | 0 | RESEARCHED_NO_REFS/EXHAUSTED_CURRENT_SOURCE |
| 43824 | DREMEL strict inputs | 3 | 0 | 0 | SOURCE_BLOCKED |

## 7. Apply eligibility (C1)

**Eligible apply scope = 0 products** (refs > 0 ни у одного). Это допустимый
partial evidence scope: A и B — независимые micro-investigations по OWNER GO,
не dynamic scope expansion. Замены (44698/43994/44911/44910) НЕ добавлялись.

## 8. Phases D–J

НЕ выполняются: нет eligible products. Manifest / backup / apply / rerun —
n/a, зафиксировано явно. ProductImage delta = 0 (global 805 → 805).

## 9. Long-tail recount (п.J) — `21-c-recount.json`

Canonical predicate воспроизведён: **remaining = 58, ожидание 58, drift 0**;
ids 1:1 с `20-baseline.json` (entered [], left []); 22673 и 43824 остаются
в remaining; ProductImage 805. Verdict PASS.

## 10. Source requests total

**4**: 1 card fetch (22673, Phase A) + 3 search loads (43824, Phase B).
Smoke NAV на главную — не source-card/search запрос, посчитан отдельно как
pre-flight. Всё в рамках GO и дисциплины.

## 11. Unrelated writes / code / config / git

Все 0. Production code коннектора и staging не менялись; новые файлы —
только scratch-скрипты фазы (21-*.py) и артефакты.

## 12. Remaining candidate observations (NO GO)

- **44698** steplery (I1): не исследовался; типовой SKM-риск (651 run-1).
- **43994** drovokoly (I1): открытый needs_review(unknown) — сначала модерация.
- **44911** stanki (I1): needs_review(unknown).
- **44910** stanki (I1): needs_review(**rejected**) — collector запрещён до
  разрешения маркера.
Ни один не получал GO; в scope не добавлялись.

## 13. GAPs

- Покрытие артикула `DRM-F0130290JZ` источником под альтернативным написанием
  (F0130290JZ) не проверялось — вне контракта B4 (query expansion NO).
- Причина отсутствия фото у 1185279 (никогда были / удалены) не установима
  без истории карточки — факт зафиксирован на свежем raw.

## 14. Recommendation

1. **VI image cycle по current source для remaining 58 исчерпан**:
   strict-capable кандидаты проверены (22673 exhausted, 43824 blocked),
   остальные 56 — SOURCE_BLOCKED/AMBIGUOUS/I3-no-research с ожиданием ≤25%
   либо moderation-флагами. Новые image-волны без новой source strategy
   (второй источник / поставщик / ручная загрузка) не предлагать.
2. Закрыть VI-INT-21 как PASS/NO_APPLY; remaining 58 заморозить для images
   до решения о source strategy.
3. Опционально отдельным треком (не image): Package B map-patch (S2=4) —
   data-only, без collector.
4. Docs closeout VI-INT-20+21 одним PR — по решению владельца.

## Verdict

**PASS / NO_APPLY.** A и B выполнены execution-корректно в рамках GO;
refs не существует (A) / карточка не существует в выдаче (B) — фактура
источника, не дефект исполнения. Staging цел, бюджет источника не затронут
существенно (4 запроса), капч нет.

---

## Addendum (OWNER decision, 2026-09-15, после написания отчёта — история не переписана)

**`VSEINSTRUMENTI CURRENT-STRATEGY IMAGE CYCLE = FROZEN`**
(evidence-bounded: для текущего remaining long-tail=58 дальнейшие
automatic waves через существующий ВсеИнструменты collector при
действующем strict identity contract не запускаются без нового
evidence/source strategy — НЕ «у источника больше нет изображений»).

Причины freeze: strict ready-to-apply inventory исчерпан ранее; 22673 —
no-photo у источника; 43824 не покрывается exact article search; 17
historical SOURCE_BLOCKED; 42072 ambiguous; broad I3 layer — слабый
прогноз yield (≤25 % — **estimate/GAP, не измеренный hit-rate**);
moderation/data-quality cases collector'ом не обходятся.

Условия разморозки (только отдельное owner decision): (1) second source;
(2) supplier/feed; (3) manual image flow; (4) new SKU-specific evidence.
44698/43994/44911/44910 — NO GO (в т.ч. 44910 blocked по
needs_review=rejected).
