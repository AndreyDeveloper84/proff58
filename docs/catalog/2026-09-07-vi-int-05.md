# VI-INT-05: RESEARCH_COMPLETE

Ruleset expansion research по промпту `docs/VI-INT-05_ruleset_expansion_research_prompt.md`.
Всё read-only: staging writes — нет, map/schema — не менялись, collector не запускался,
Attribute/Option не создавались, git — нет. Артефакты: `/home/taximeter/vi02/05-*`
+ `scratchpad/vi-int-02/05-*` (включая воспроизводимые скрипты `05-part1/2/3-*.py`).

## 1. Current remaining scope

- Long-tail remaining после Batch A: **101** товар (121 актуальный пул − 20 Batch A v2;
  id-список — `05-baseline.json`).
- Map-covered (tool_type ∈ scope карты, 30 типов): **16**.
- Strict collected ready (in-scope, карточки на диске): **4** (22673, 36920, 36921, 13002).
- Identity/source coverage по 101: с strict-карточками **21** (4 in-scope + 17 вне-scope),
  SKM не-кандидаты **13** (42055/11093 — повторный промах в run-2), CONFLICT **2**,
  без карточек **65**.
- Собранные карточки: **66 MATCHED** (pilot-30 25 + run-1 23 + run-2 18).

## 2. Tool_type coverage gap (G-3 разложен)

- **A. TOOL_TYPE_COVERAGE — доминирующий блокер: 85 товаров (39 типов)** вне scope.
  Из них 17 уже имеют собранные strict-карточки (лежат на диске — enrichment без
  нового сбора при расширении scope).
- **B. ATTRIBUTE_MAPPING_COVERAGE:** по собранным in-scope картам из 43 значений
  mapped только **8** — карта тонкая даже внутри scope (13 mapped полей из 184).

## 3. Tool_type ROI ranking (top)

| tool_type | in_scope | prod | strict | skm | chars | safe PAV | confidence |
|---|---|---|---|---|---|---|---|
| boltorezy | | 5 | 2 | 0 | 7 | 0 | medium |
| izm-dalnomery | | 5 | 0 | 0 | 0 | 0 | low |
| siz-nakolenniki | | 5 | 0 | 0 | 0 | 0 | low |
| kobury-dlya-instrumenta | | 4 | 0 | 0 | 0 | 0 | low |
| shtoborezy | | 4 | 0 | 0 | 0 | 0 | low |
| nabory-pilok | | 3 | 2 | 1 | 12 | 1 | high |
| dinamometricheskie-klyuchi | ✓ | 3 | 1 | 2 | 12 | 2 | high |
| miksery | | 3 | 1 | 0 | 20 | 2 | high |
| otboynye-molotki | | 3 | 1 | 0 | 13 | 2 | high |
| machete-i-sekachi | | 3 | 1 | 0 | 11 | 1 | high |

Полная таблица — `05-tooltype-roi.json/.md`. Вывод: максимальный прирост при
минимальном расширении — типы с готовыми картами и safe PAV > 0; большие типы без
карточек (izm-dalnomery, siz-nakolenniki) — только после research-сбора.

## 4. Attribute mapping ROI ranking

Топ по охвату (полная таблица `05-attr-audit.json/.md`, verdict'и по шкале промпта):
- SAFE_MAP_EXISTING: «Квадрат»→drive, «Количество в наборе»→piece_count,
  «Max усилие»→torque, «сталь»→material.steel (уже в словаре).
- SAFE_SELECT_EXTENSION: CrV/Cr-V (alias + 1 option), «полипропилен», «хвостовик Т» (t-shank).
- NEW_ATTRIBUTE_CANDIDATE: «Класс товара», «Трещотка» (bool), «Цвет», «Min усилие»,
  механизм dinamometricheskie — блокер: policy «schema не создаётся».
- NEEDS_UNIT_RESEARCH: «Объем», «Длина» у fiksatory-germetiki-rezby.
- SEMANTICALLY_AMBIGUOUS: «Назначение» (canonical ×3), «Тип патрона» М27, «Вид».
- DROP/INSUFFICIENT: «Диэлектрическое покрытие» (все 8 = «нет»), «Габариты без упаковки»,
  «Упаковка», «Батарейки», «металл», «Состав».

## 5. Material audit

Canonical options: 20. Source-варианты по 66 картам (`05-material-audit.json/.md`):
**CrV ×7 + Cr-V ×1**, полипропилен ×4, сталь ×2, пластик ×1, металл ×1, композит ×1.
- **Cr-V == CrV — одна доказанная сущность** (строка с/без дефиса, инструментальная
  сталь ключей/полотен) → proposal: normalization alias + **одна** новая option
  «Хром-ванадиевая сталь»; две options НЕ создавать; map в родительский
  «Легированная сталь» — не рекомендуется (потеря точности).
- «сталь» → точный canonical `steel` (уже покрыто словарём карты).
- Изделие vs рабочая часть: generic material корректен для лент/каскеток/проволоки/
  накладок/инструментальной стали ключей; borderline — nabory-pilok (материал полотен).
- «металл» — шум (REJECT); композитная строка — INSUFFICIENT_EVIDENCE.

## 6. «Тип» semantic audit

19 товаров, 13 контекстов (`05-type-audit.json`). Generic `type` — никогда.
Однозначные локальные семантики: dinamometricheskie = **механизм** («щелчковый» ×3),
nabory-klyuchey-imbusovyh = **тип шлица** (Torx → bit_type). Остальное — вид изделия/
дубль tool_type (лента, лен, шланг) → DROP, либо назначение → AMBIGUOUS. Одно и то же
source name имеет разные смыслы у разных tool_type → только per-tool_type mappings.

## 7. «Длина» / units audit

15 строк (`05-length-audit.json`). Global DIRECT — запрещён, unit-trap подтверждена:
**krep-provoloka «Длина: 100» при названии «100м»** — метры мотка, не мм.
Классы: безопасное mm → `length` (boltorezy 350/300, trubogiby 460; obor-shlangi 290 —
medium); рулон в м → canonical `tape_length` (unit м, точно: ленты 30/50/100 м,
конверсия не нужна); fiksatory «55/110» — NEEDS_UNIT_RESEARCH.
Реализация unit conversion — только research result, не сейчас.

## 8. Остальные high-frequency поля

«Диэлектрическое покрытие» — INSUFFICIENT_EVIDENCE (все «нет»); «Класс товара» —
NEW_ATTRIBUTE_CANDIDATE (шкала из 3 опций); «Трещотка» — NEW_ATTRIBUTE_CANDIDATE bool;
«Тип инструмента» — **taxonomy axis, НЕ PAV** (только validation signal; скрытое
изменение taxonomy через importer недопустимо); «Тип патрона» — sds-max→chuck SAFE,
М27 — REJECT (не патрон); «Тип хвостовика» — SDS-plus→shank_type SAFE, «хвостовик Т» —
новая option (Pack B, 1 товар); «Квадрат» → drive SAFE; «Объем» — REJECT (unit-chaos).

## 9. Targeted collector research

Для Pack A SAFE NOW доказательств достаточно. Для per-tool_type решений (1–2 карточки
на тип) и типов с 0 картами — **недостаточно**. Подготовлен (НЕ запущен) targeted pool:
**16 товаров** × 8 типов (miksery, otboynye-molotki, boltorezy, machete-i-sekachi,
zap-filtry, bp-podgotovka-vozduha, izm-dalnomery, siz-nakolenniki — по 2;
SKM/CONFLICT-промахи исключены программно), ~51 loads ≈ 6 сессий ≈ 1–2 ночи по
существующему контракту (`05-data-sufficiency.json`).

## 10. Pack A — SAFE NOW

`05-ruleset-pack.json/.md`. Состав:
- **Map field additions (3, плоские):** «Квадрат»→drive (select, словарь дюймов),
  «Количество в наборе»→piece_count (decimal шт), «Max усилие»→torque (decimal Н·м).
- **material values additions (3 ключа → 2 новые options, proposal):**
  `crv`/`cr-v` → crv-steel (alias, 1 option «Хром-ванадиевая сталь»),
  `полипропилен` → polipropilen.
- **scope_tool_types additions (10, выведены replay'ем — у всех safe PAV > 0 на
  лежащих картах):** bp-motopompy, bp-nabory-pnevmoinstrumenta, elektronozhnitsy,
  machete-i-sekachi, miksery, nabory-burov, nabory-pilok, obor-shlangi,
  otboynye-molotki, zap-filtry.
- Regression fixtures: Batch A v2 dry-run 1:1; 13002 (Квадрат+Max усилие+CrV);
  4667 (Cr-V→crv-steel); 36920/36921 (полипропилен).
- Negative fixtures: «Длина» '100' (моток), «Длина» '50 м' лент, «Тип» любые,
  «Тип инструмента», «Объем» '5.8 кг', «Материал»='металл', «Min усилие» → не torque.
- Риски: низкие; словарные select'ы с on_ambiguous=drop_to_report — неизвестное
  значение уходит в отчёт, а не в данные.

## 11. Pack B — RESEARCH LATER

«Тип» и «Длина» per-tool_type mappings (**блокер: карта плоская — нужна schema v2
с per-tool_type overrides + код importer'а**); новые атрибуты (Трещотка, Класс товара,
Цвет, Min усилие — блокер: policy «schema не создаётся»); «хвостовик Т» и «пластик»
(1 товар — INSUFFICIENT_EVIDENCE, добор через research pool); «Объем» units research;
«Диэлектрическое покрытие»; типы без карточек (ждут pool из п.9).

## 12. Pack C — REJECT

«Тип инструмента» (taxonomy), generic `type`, «металл», «Габариты без упаковки»,
«Упаковка»/«Вид упаковки», «Батарейки», «Состав», «Назначение» глобально (canonical ×3),
«Тип патрона» М27, «Объем» — в canonical schema/map не попадают.

## 13. Offline simulation: CURRENT vs AFTER PACK A

| метрика | CURRENT | AFTER PACK A | Δ |
|---|---|---|---|
| remaining products | 101 | 101 | 0 |
| map-covered products | 16 | **36** | **+20** |
| strict collected covered | 4 | **15** | **+11** |
| estimated safe PAV | 4 | **24** | **+20** |
| products meaningfully enriched | 3 | **14** | **+11** |
| dropped | — | −2 | |
| unmapped | — | +93 (новые in-scope карты приносят свой unmapped-хвост — механика scope, не регресс) | |

Replay — `05-simulation.json/.md` (детерминированный, по 66 картам на диске).
Per-product AFTER: 13002 → 4 PAV; 22673, 28587, 36920, 36921, 44142, 44175, 44388 → 2;
1782, 2916, 4667, 7326, 35805, 40820 → 1.

## 14. Ожидаемый PAV/product gain

**+20 safe PAV и +11 товаров enrichment без единого нового запроса к источнику**
(карточки уже собраны). Покрытие scope по remaining: 16 → 36 товаров (+125 %).
Прирост именно товаров, а не только mappings: 3 → 14 meaningfully enriched.

## 15. Recommendation

**R1** — реализовать Pack A (PR → CI → deploy), затем Batch B. Обоснование числами:
Pack A даёт +20 PAV / +11 товаров / +20 covered при нулевой стоимости сбора и низком
риске (3 плоских поля + 2 options + 10 scope-типов, всё с regression/negative
fixtures). R2 отброшен: бесплатный прирост терять незачем. R3 — не блокер для Pack A,
но **обязателен следующим шагом** для основного приза (per-tool_type mappings —
«Тип»/«Длина», новые атрибуты, типы без карт): targeted pool из 16 товаров готов.

## 16. Proposed scope следующего implementation track

**Track 1 (Pack A, PR):**
- `data/catalog_processing_rules/scraped_attr_map.vi-longtail.json`: +3 fields
  (Квадрат/Количество в наборе/Max усилие — дословно по `05-ruleset-pack.json`),
  +3 ключа в values «Материал» (crv, cr-v, полипропилен), +10 scope_tool_types.
- 2 новые AttributeOption (crv-steel «Хром-ванадиевая сталь», polipropilen
  «Полипропилен») — миграцией, как huter/vseinstrumenti ранее.
- Regression: Batch A v2 dry-run 1:1 (20/20, create 23) + fixtures из п.10;
  negative fixtures — явные тесты не-маппинга.
- Затем Batch B (состав — отдельным решением; 15 covered strict-карт уже на диске).

**Track 2 (после Track 1):**
- Map schema v2: per-tool_type overrides полей + код importer'а (для «Тип»/«Длина»);
- targeted collector research pool (16 товаров, `05-data-sufficiency.json`) —
  запуск по обычному runbook'у;
- решение по NEW attributes (Трещотка/Класс товара/Цвет/Min усилие) — отдельный
  schema-трек (policy «schema не создаётся»).

После отчёта — стоп. Ничего не применять, Batch B не запускать.
