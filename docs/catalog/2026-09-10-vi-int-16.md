# VI-INT-16 — Package A / Pure Scope Expansion → **PASS**

Дата: 2026-09-10. Продолжение `2026-09-10-vi-int-15.md` (RESEARCH_COMPLETE,
принята рекомендация R1). Два гейта по промпту: Phase A (implementation:
+5 tool_type в scope, regression, PR → CI → merge → deploy) → Phase B
(post-deploy dry-run gate → controlled apply 11 PAV). Данные остаются на
staging.

## Вердикт: PASS

- CREATE 11 == immutable manifest точно · CONFIRM 0 (22673 исключён по
  п.34 промпта — объяснимое отличие) · PROTECTED/skipped_voltage 4 ·
  conflicts 0 · unauthorized updates 0 · higher-priority overwrite 0 ·
  schema/taxonomy/image zero-write · foreign mutations в scope 0 ·
  rerun = полный no-op · boltorezy negative control PASS.

## 1. Initial contract

Расширить deployed `scope_tool_types` ровно на 5 доказанных типов
(payalniki, stameski, stanki-derevoobrabatyvayushchie, sverlilnye-stanki,
ustanovki-almaznogo-bureniya), scope 40→45; mappings/normalization/units/
source priority/matcher/importer/protection/prune — без изменений.
Канонические корректировки VI-INT-15: voltage ×4 = PROTECTED;
22673 piece_count=7 = уже применён.

## 2. Exact scope diff

`scraped_attr_map.vi-longtail.json`: ровно +10 строк (2×5 — обе копии,
top-level и policy, идентичны). Больше в production config — ничего.

## 3. Regression changes

- `test_vi_pack_a.py`: assertion scope=45, exact additions поимённо
  (10 Pack A + 5 VI-INT-16 раздельными множествами), без случайного
  расширения;
- `test_vi_scope_expansion.py` (новый, 23 теста) + fixture
  `vi_scope_expansion_cards.json` (литеральные attributes карт Wave 2A).

## 4. Voltage negative tests

Все 4 кейса (24740 «220», 44908 «220», 44928 «230», 44985 «220»):
non-battery ≥60 В → extraction + `not is_battery_values` →
skipped_voltage/PROTECTED по production-правилу (код не менялся);
обратный контроль battery 18 В.

## 5. Generic fail-closed tests

«Тип», «Тип инструмента», generic «Длина», «Класс товара», «Цвет»,
«Механизм», «Min усилие», generic «Объем» — 0 импорта после расширения;
Package A не превращается в B/C/D.

## 6. Unit/select audit

S1-значения — прямые единицы (Вт/кг/мм/Н·м/шт), конверсий нет; unknown
select → dropped, option не создаётся; новых options 0.

## 7. Production/config diff

Только +5 scope entries (+assertion + новый test/fixture). Любых других
production changes = 0; `makemigrations --check` — no changes.

## 8. PR

**#732** → dev: основание VI-INT-15 (S1=16, корректировки voltage/22673),
exact diff, regression, запреты. Без VI-INT-15 report closeout (отдельным
docs PR позже), без Package B/C/D.

## 9. CI

4/4 green (lint 18 с, frontend 1m13s, test 5m52s, catalog-rules-gate
33 с). Инцидент: первый `pull_request` run не стартовал (GitHub не
породил check-runs — инфра-событие, не код); retrigger close/reopen —
workflow прошёл штатно.

## 10. Merge SHA

`31b6c7d21581c3e0663009854bc01177bd09c086`.

## 11. Deploy status/SHA

Run 34444300216 — **completed/success**; host checkout == `31b6c7d2`;
containers healthy (8 Up).

## 12. Deployed scope verification

scope = **45** (обе копии); 5 additions поимённо присутствуют; случайного
расширения нет; map hash `c95ee2e2…` (наш PR); rules hash отличается от
15-contract — **foreign evolution** (merge #731, power_hp axis), не наше
— зафиксировано.

## 13. Post-deploy revalidation

6/6 ok (active/stock/article/brand/tool_type/identity/content_locked);
images — наши VI-INT-13 (main ×1 у каждого); write scope = 5 (без
31957).

## 14. Post-deploy dry-run (главный execution gate)

**CREATE = 11** (1:1 контракт): 24740 power 40 + weight 0.136; 33229
weight 1.31; 44908 power 800 + diameter 200 + bore 30 + weight 13.4;
44928 width 225 + height 580 + weight 16.4; 44985 weight 23 · CONFIRM 0 ·
**PROTECTED = 4** (все voltage → skipped_voltage) · conflicts 0 ·
S2/S3/S4 writes 0 · unknown options 0 · DB drift 0.

## 15. Forecast vs actual plan

Forecast VI-INT-15: 11 CREATE / 1 CONFIRM / 4 PROTECTED. Actual: 11/0/4.
Отличие CONFIRM 1→0 объяснено: «1» относился к 22673 piece_count=7, а
22673 по п.34 промпта не включён в write scope — класс «не в scope
экспорта», не дефект.

## 16. Hard-stop audit

Чисто: ни одного условия п.17 (unexpected product/type, S2–S4 write,
voltage CREATE, conflict, unknown select, protected mutation, prune,
mapping drift, scope >45) не сработало.

## 17. Immutable write manifest + SHA

`16-write-manifest.json`: 11 CREATE-операций (product/article/tool_type/
source_field/canonical_attribute/normalized_value/expected_action/source)
+ 4 PROTECTED как non-write outcomes. **SHA256
`1cf4a7347d2d5aef7dc474e25a2ec7069bb394acc6a6cec9e0bf8715e040b712`**;
runner проверял перед write.

## 18. Global/scope baseline

Global: Attribute 108, Option 671, CatAttr 255, PAV 78946, ProductImage
787, migrations 127. Scope: по 5 — exact PAV, attrs_cache, images,
source ownership; по 11 операциям — exact before state.

## 19. Backup + validation

`db-20260910-094312-pre-vi-int16.dump` (22 724 526 б), sha256
`d084fa23…262b`, exit 0, TOC 733 / TABLE DATA 72, persistent, df ≥2 ГБ.

## 20. Pre-write drift gate

**CLEAN**: повторный dry-run план == immutable manifest операция в
операцию (11 CREATE + 4 PROTECTED побайтово).

## 21. Apply result

`ЗАПИСЬ (PAV: 11)`, matched 5/5. CONFIRM/PROTECTED не создали PAV.

## 22. Exact created PAV

**11**: 24740 power=40, weight_kg=0.136 · 33229 weight_kg=1.31 · 44908
power=800, diameter=200, bore=30, weight_kg=13.4 · 44928 width=225,
height=580, weight_kg=16.4 · 44985 weight_kg=23.

## 23. CONFIRM

0 на apply (см. п.15); на rerun — 11 (первоначальные CREATE → CONFIRM).

## 24. PROTECTED/skipped_voltage

**4** (24740, 44908, 44928, 44985 — voltage не записан ни разу); на rerun
остались PROTECTED.

## 25. Source ownership

Все 11 новых PAV — `source='scraper'`; manual/rules/import_1c/llm-owned
не тронуты; higher-priority не перезаписаны.

## 26. attrs_cache audit

Пересчитан штатным `_cache_from_pavs` по затронутым; **mismatch 0**.

## 27. G-4 attribution

**global PAV delta +11 == exact ours +11** (по manifest/provenance);
foreign delta в окне = 0; foreign mutations внутри 5 = 0 (per-product
diff). ImportRun 136/137 — журнал.

## 28. Foreign mutations

0 в окне write.

## 29. Mandatory rerun — полный no-op

`ЗАПИСЬ (PAV: 0)`: CREATE 0, CONFIRM 11, PROTECTED 4, conflicts 0,
deletes/prune 0, attrs_cache unchanged, PAV delta 0.

## 30. Schema zero-write

Attribute 108=108 · Option 671=671 · CategoryAttribute 255=255 ·
migrations 127=127 · taxonomy не изменилась.

## 31. Image zero-write

ProductImage 787=787; наш delta 0 (characteristics-only); VI-INT-13
изображения не тронуты.

## 32. Boltorezy negative control

31957: scope match = **False** · characteristics write = 0 (PAV count = 1,
только tool_type) · изображение VI-INT-13 (id 857, vseinstrumenti,
is_main, checksum `83ff2d7e…`) побайтово неизменно.

## 33. Long-tail recount

**64 — без изменений** (по predicate): PAV-only не меняет предикат; 5
write-товаров уже выбыли по фото (VI-INT-13). Eligible: 22673.

## 34. Package B/C/D — unchanged

S2 = 4, S3 = 7, S4 = 9 — research inventory, реализаций нет.

## 35. Old strict — untouched

6 old strict (3 refs ×6) не применялись — отдельный будущий image-only
track.

## 36. 22673 — untouched

piece_count = 7 уже есть; remaining char yield = 0; refs = 0; writes 0.

## 37. Новые GAP/incidents

- **I-1 (инфра):** GitHub не породил CI run на первый `pull_request`
  (#732) — retrigger close/reopen решил; не код.
- **F-1 (наблюдение):** `attribute_rules.json` на staging отличается от
  VI-INT-15 contract — foreign merge #731 (power_hp axis); не наше
  изменение, зафиксировано как foreign evolution. При будущем
  load_attributes учитывать, что rules-файл эволюционирует чужими
  треками.
- По коду/данным новых GAP нет.

## 38. Recommendation

Не запускать следующий track автоматически. Варианты (отдельный OWNER
GO): (1) **old strict image cleanup** (6 карточек × 3 refs — image-only,
самый дешёвый оставшийся yield); (2) **docs closeout** VI-INT-15/16
отчётов; (3) новый collector (Wave 2B gravery + пересмотр 2C-слоя);
(4) Package B map research / schema-v2 research. По дисциплине: (2) → (1).

## Артефакты

19 файлов `16-*`: `/home/taximeter/proff58-staging/var/vi02/` + локально
`proff58/scratchpad/vi-int-02/` (postdeploy-verification, revalidation,
dryrun, write-manifest+sha256, baselines, recovery-plan, drift-gate,
apply, attribution, postaudit-final).
