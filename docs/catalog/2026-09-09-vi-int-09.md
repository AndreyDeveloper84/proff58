# VI-INT-09 — Combined Controlled Batch (13 research-strict) → **PASS**

Дата: 2026-09-09. Продолжение `2026-09-09-vi-int-08.md` (RESEARCH_COMPLETE,
рекомендация R4 принята владельцем). Combined apply: изображения для всех
13 + характеристики по текущему deployed ruleset. Код/map/schema не
менялись. **schema v2 = DEFER · Track 2 = NOT STARTED · new collector =
NOT STARTED · Batch C = NOT STARTED.**

## Вердикт: PASS

- strict identity 100 % (в scope) · false assignment 0 · conflicts 0 ·
  unit-trap 0 · semantic mismatch 0 · schema expansion 0 · taxonomy write 0 ·
  higher-priority overwrite 0 · existing mutations 0 · duplicate main 0 ·
  duplicate images 0 · orphan files 0 · foreign mutations в scope 0 ·
  rerun = полный no-op (downloads 0).

## 1. Revalidated scope

13/13 clean (`09-revalidation.json`): все exists, active+stock, article ==
карточке, brand/tool_type == карточке, source card та же, strict identity
валиден, content_locked=False, images 0, main 0, открытых processing
items 0. PAV state: 11×1, 10608/10610×2, 44177×4.

## 2. Drift относительно VI-INT-08

**Отсутствует.** Замен не потребовалось.

## 3. Immutable manifest + SHA256

`09-batch-manifest.json`, 13 items, deterministic ordering, 37 refs,
**SHA256 `e02167d7da8fa770f6bcff2b15b04d55a4d3ec0279921076f4b478aee4f19751`**
(runner проверял при каждом write — `MANIFEST_SHA256_OK`).

## 4. Characteristics dry-run

matched **5/13** (in-scope: miksery ×2, otboynye-molotki,
machete-i-sekachi, zap-filtry); not_found 8 — в точности out-of-scope
(boltorezy, izm-dalnomery, bp-podgotovka-vozduha, siz-nakolenniki) —
scope не обходился, норма. **CREATE 6** (42071 weight 0.151; 44144
power 1400 + weight 5.3; 44145 power 1600 + weight 5.6; 44177 weight
26.1) + **CONFIRM 1** (44177 power 1800, source=regex — защита) = 7
значений. dropped 93, unmapped 31, conflicts 0, skipped_voltage 0. DB
drift после dry-run = 0.

## 5. Current-map scope verification

Таблица по всем 13: in-scope 5 (importer matched ✓), out-of-scope 8
(not_found ✓). Write выполнен **только через deployed scope** —
hypothetical 11 PAV без scope-фильтра не применялись.

## 6. Fail-closed audit

FAIL_CLOSED_OK: created_attrs только {power, weight_kg}; «Тип»/«Тип
инструмента»/«Длина»/«Min усилие»/«Объем» не импортируются; 2
dropped_select fail closed (options не созданы); **«Тип патрона»
М27/М14 у miksery в created отсутствует — REJECT-фикстура VI-INT-08
подтверждена**.

## 7. Image preflight

refs **37** (min 1 / median 3 / max 3) · existing images 0 · existing
vseinstrumenti 0 · existing main 0 у всех 13 · source_url collisions 0 ·
input дублей 0 · invalid (M-13) 0 · expected downloads 37.

## 8. G-4 global baseline

`09-baseline-global.json`: Attribute 107, Option 628, PAV total 73287
(+ source dist), ProductImage 734 (+ source dist), migrations 127
(staging живой: +7 attrs/+27 options/+4,7k PAV чужих треков со Batch B —
зафиксировано как baseline, не как наш delta).

## 9. G-4 scope baseline

`09-baseline-scope.json`: по 13 — exact PAV set (все поля + source),
attrs_cache, exact ProductImage set (= пусто).

## 10. Backup + SHA256 + structural validation

`db-20260909-104251-pre-vi-int09.dump` (22 644 585 б), sha256
`eb48709c…98df`, exit 0, TOC 733 / TABLE DATA 72, persistent, df ≥2 ГБ.
Снят после freeze, непосредственно перед write.

## 11. Recovery plan

`09-recovery-plan.md`: provenance-based. PAV — exact tuples
(product_id, attribute, source='scraper') с доказанным пустым пересечением
с scope-baseline; images — `catalog_images_ops --source vseinstrumenti
--since <окно>` + foreign records = 0. Без диапазонов id.

## 12. Pre-write drift gate

**CLEAN** — 13/13 без изменений против frozen manifest
(`09-pre-write-drift-gate.json`).

## 13. Characteristics apply

`ЗАПИСЬ (PAV: 6)`, matched 5/13 (in-scope). Guard: proff58_staging +
VI03_STAGING_PILOT=1.

## 14. Characteristics attribution

**global PAV delta +6 == VI-INT-09 exact delta +6; foreign PAV delta в
окне = 0; foreign mutations внутри 13 = 0** (per-product diff +
source-разрез).

## 15. Characteristics post-audit — ALL_OK

created **6 == approved dry-run плану точно** · confirm 1 (защита regex) ·
unauthorized updates/deletes 0 · все 6 source='scraper' ·
schema/taxonomy delta 0 · attrs_cache == факту · conflicts 0 ·
ImportRun 116 (журнал).

## 16. Images apply

Существующий `ImagePipeline.process_batch` (код не менялся). Пилот 44144 →
полный прогон: created 34 + dedup 3 = **37/37**, rejected 0.

## 17. Image attribution

**global ProductImage delta +37 == exact +37; foreign image delta в
окне = 0.**

## 18. Image post-audit — ALL_OK

affected ⊆ 13 · provenance полный (source_url/checksum==sha256
файла/fetched_at) · existing image/main mutations 0 · dup
(product,source_url) = 0 · dup (product,checksum) = 0 · introduced
main>1 = 0 · **main distribution 0/13/0 — у всех 13 ровно 1 main =
первое созданное** · orphan files 0 · ordering (-is_main, sort_order, pk).

## 19. Mandatory rerun — полный no-op

chars: `ЗАПИСЬ (PAV: 0)` create 0 / update 0 / confirm 7 (idempotent,
ImportRun 117) · images: created 0, все 37 dedup **до сети** (downloads 0,
окно 0,15 с) · main flags побайтово не изменились · `migrate --plan`
пуст.

## 20. Final integrity audit — ALL_OK

exact PAV/images before/after по 13 · provenance/checksums полны ·
attrs_cache == факту · schema counts неизменны (Attribute 107,
Option 628, CatAttr 255, migrations 127) · source_dist: vseinstrumenti
**196** (159+37), прочие без изменений · 8 контейнеров healthy.

## 21. Actual PAV yield

**6 PAV** (4 товара): power ×2 (1400, 1600), weight_kg ×4 (0.151, 5.3,
5.6, 26.1); +1 confirm (защита, не запись).

## 22. Actual image yield

**37 изображений** (13 товаров).

## 23. Products meaningfully enriched

**13**: 4 — характеристиками+изображениями, 9 — изображениями (image-first
стратегия R4 сработала как рассчитано: основной yield — изображения).

## 24. Actual long-tail recount

Пересчёт по predicate (не «86−13»): **remaining 101 → 70**.
Декомпозиция выхода: Batch B по фото 15 + VI-INT-09 по фото 13 +
foreign-треки 3 (= 31; 101−31=70 ✓). pool 6148, tail types 116.
Без изображений остаются 70.

## 25. Remaining products with strict cards

**1** — 22673 (Batch B, без refs: характеристики применены, изображений
нет). Unused in-scope strict cards кроме него = **0**.

## 26. Remaining products without source cards

63 (из 70 remaining; +11 SKM/CONFLICT не-кандидатов, +57 out-of-scope по
пересечениям классов — `09-longtail-recount.json`).

## 27. Foreign staging activity during write window

**0** в окне write (global deltas == нашим exact deltas). Staging в целом
живой (baseline-факт п.8), но в наше окно конкурентных записей не было.

## 28. G-4 attribution verdict

Чистая атрибуция по новому постоянному правилу: истина = manifest +
provenance + per-product diff; global counters — только вспомогательный
сигнал; foreign mutations доказуемо 0.

## 29. Новые GAP / incidents

Нет.

## 30. load_attributes drift = untouched

1 attribute update + 8 bindings — не применялись, не чинились; полный
`load_attributes` не запускался.

## 31. Schema v2 = NOT STARTED (DEFER)

## 32. Track 2 = NOT STARTED

## 33. New collector = NOT STARTED

## 34. Batch C = NOT STARTED

## 35. Recommendation следующего шага

- **Strict-carded запас исчерпан** (кроме 22673 без refs — images для
  него недоступны источником). Любое дальнейшее enrichment требует нового
  сбора: 63 remaining без карт.
- Естественные варианты (отдельный OWNER GO):
  (а) **collector wave 2 по remaining 63** (стратификация по
  `05-tooltype-roi`: boltorezy-остаток, izm-dalnomery-остаток,
  siz-nakolenniki-остаток, kobury, shtoborezy…) — ~200 loads ≈ 4–6 ночей
  по контракту; ожидаемый yield по статистике: ~80 % strict, images
  ~2,8/товар + chars по расширенной карте;
  (б) **Scenario C schema-track** (Класс товара/Цвет/Трещотка/Min
  усилие/механизм — +43 потенциальных PAV, в основном добор applied) —
  отдельный schema track с PR;
  (в) **Pack B schema v2** — DEFER до созревания ROI (gain ~9 PAV на
  remaining);
  (г) пауза/консолидация: отчёты трека в docs PR (VI-INT-06..09 лежат
  незакоммиченными в worktree).
- По дисциплине рекомендую (г) → (а): сначала оформить документацию,
  затем collector wave 2 как следующий evidence-шаг.

## Артефакты

28 файлов `09-*`: `/home/taximeter/proff58-staging/var/vi02/` + локально
`proff58/scratchpad/vi-int-02/` (revalidation, manifest+sha256, dry-run,
failclosed-audit, image-preflight, baselines, recovery-plan, drift-gate,
apply/postaudit ×2, rerun, postaudit-final, longtail-recount).
