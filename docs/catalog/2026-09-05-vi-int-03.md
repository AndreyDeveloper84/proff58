# VI-INT-03 — push → PR → CI → merge/deploy → staging pilot (25 fixture) → **PASS**

Дата: 2026-09-05. Продолжение `2026-09-04-vi-int-02a.md`. Протокол владельца
выполнен полностью. **Long-tail 128 НЕ запускался** — следующее
OWNER-решение (controlled rollout, размер батча) отдельно.

## Вердикт: PASS

25 fixture-товаров на staging обогащены характеристиками (26 PAV,
`source='scraper'`) и изображениями (69 фото, `source='vseinstrumenti'`)
без единого нарушения инвариантов. Данные **не откатываются** — первый
реальный результат интеграции. Recovery-пакет сохранён.

## Secondary ordering fix

- `ProductImage.Meta.ordering = ["-is_main", "sort_order", "pk"]` —
  детерминированный tie-breaker; существующие sort_order и данные не
  меняются, UPDATE не требуется. Миграция `0040_alter_productimage_options`
  (только Meta options).
- Regression test `test_secondary_images_order_stable_by_pk`: secondary при
  равном sort_order стабильны по pk, единственный main — первый. PASS.

## Коммиты (ветка feature/vi-int-01-scraped-import)

- `e90ec41` VI-INT-01 адаптер · `a553b71` VI-INT-01a блокеры ·
  `5d9d305` VI-INT-02/02a (main-инвариант, 0039, R-1) ·
  `827bcb4` fix deterministic ordering (+0040) ·
  `fa29d3b` style black (CI lint gate).
- В PR вошли 8 коммитов базы ДРФ-1439/1440 (`8f67e4f…0ef21f4`) — фактически
  уже работали на staging; зафиксировано в теле PR.

## PR и CI

- **PR #682 → dev**: полное описание (зачем VI-track, почему не второй
  importer, identity 25/25, 26 safe INSERT, moderation не применяется,
  ImageSource.VSEINSTRUMENTI, main-инвариант, deterministic ordering,
  миграция 0039, rollback-контракт, sandbox-результаты, запрет long-tail
  128, ссылки на отчёты VI-INT-01/01a/02/02a).
- **CI 4/4 green**: lint 15 с, frontend 1m27s, test (полный suite) 5m17s,
  catalog-rules-gate 19 с. Первая попытка: lint FAIL (black, 4 файла
  VI-трека) — исправлено `fa29d3b`, классификация: кодовый дефект
  форматирования, не инфра.
- Git hygiene перед push: `0038_imagesource_huter.py` побайтово == blob
  `5a7646b5` из `origin/feature/catalog-huter-images` (в target-ветках dev
  файла нет); migration graph: план ровно 0039+0040; `makemigrations
  --check` чист; pre-push suite на реальном PostgreSQL — **3066/0/0/1**.

## Merge / deploy

- **Merge SHA:** `de1241249b00f6fdd4164785213a3ba5601f587a` (PR #682, merge
  commit, 2026-09-05 05:24 UTC).
- **Deployed staging SHA:** `35a929ec` — включает `de124124`; дельта —
  только `.dockerignore` (PR #684 главного окна: исключены backups/ и
  scratchpad/ из build context — корень таймаутов 940 МБ).
- **Deploy — инфра-инцидент (классификация: ENVIRONMENT, не код):** два
  запуска deploy.yml падали по `command_timeout: 30m` на передаче
  build-context 940 МБ (~1,5 МБ/с, перегруженный хост); удалённые скрипты
  продолжали, но сборки умирали молча. Выполнен workaround: тот же скрипт
  deploy.yml (build → release.sh → up → nginx reload → smoke_csp → prune)
  запущен на хосте через nohup с полным логом
  (`/home/taximeter/vi02/deploy-manual.log`) → `DEPLOY_MANUAL_OK`.
  Тесты в обоих deploy-прогонах — зелёные; красные статусы run'ов в
  Actions — артефакт таймаута SSH-сессии.

## Post-deploy verification (read-only) — PASS

- checkout `35a929e` ⊇ merge; web/nginx пересозданы, все контейнеры healthy;
- миграции **0039 + 0040 applied** (showmigrations), `makemigrations
  --check` чист;
- `ImageSource`: huter и vseinstrumenti доступны; huter-данные (62) целы;
- baseline counts: PAV 66107, ProductImage 575, Attribute 94, Option 546,
  CatAttr 222, Products 47225; `source=vseinstrumenti` = 0 до pilot;
- deploy сам данные каталога не менял.

## Pre-pilot backup

`/home/taximeter/backups/staging/db-20260905-110136-pre-vi-int03-pilot.dump`,
sha256 `6d4f1d47…2c22c0`; валидация структурой: TOC 733, TABLE DATA 72 ==
предыдущему принятому дампу. Baseline B2 (`03-baseline-B2.json`, sha256
`9a74ae27…bc90e`) — непосредственно перед apply (staging живой: утром
работал трек ДРФ-1438).

## Staging dry-run — PASS

matched **25/25**, ambiguous 0, false assignments 0; create 26 / dropped 11 /
skipped_voltage 4 — **точно** fixture expectation (дрейфа staging в этих 25
не оказалось); отчёт поэлементно == эталону VI-INT-01a; DB после dry-run ==
B2 побайтово. Guard: `current_database()=proff58_staging` +
`VI03_STAGING_PILOT=1` — во всех write-запусках.

## Characteristics apply — PASS

planned 26 → **created 26** (17 товаров), already_present/confirm 0,
protected: skipped_voltage 4 + dropped 11 (не применены), **conflicts 0**.
Новых Attribute/Option/CatAttr = 0; перезаписей приоритетных PAV = 0
(программный diff vs B2); все новые PAV `source='scraper'`; attrs_cache
25/25; ImportRun 85 — журнал.

## Images apply — PASS

Существующий `ImagePipeline.process_batch` (код не менялся). Пилот 2092:
3 созданы, все `is_main=False`, main 615 не тронут (сценарий A). Полный
прогон: created 66 + dedup 3 = **69/69**, rejected 0 (~3,5 мин, троттлинг).
Аудит: затронуты только 25 fixture; provenance полный (source_url/checksum/
fetched_at), checksum == sha256 файла; dup (product,source_url) и
(product,checksum) = 0; **main>1 = 0**, existing mains 615/641/643 побайтово
целы; у 22 без main — ровно 1 main = первое созданное; ordering
детерминирован; orphan files 0; 14 legacy-товаров без main — не тронуты;
source_dist: +vseinstrumenti 69, остальное без изменений.

## Rerun / no-op (production-like idempotency) — PASS

- chars: `ЗАПИСЬ (PAV: 0)` — create 0 / update 0 / confirm 26;
- images: created 0, все 69 dedup **до сети** (скачиваний 0, окно 0,23 с);
- schema 0 (`migrate --plan` пуст); main flags побайтово не изменились.

## Post-audit (финальный) — ALL_OK

matched 25 · improved chars 17 · PAV created 26 · images created 69 ·
products improved images 25 · moderation зафиксирован, не применён
(dropped 11, skipped_voltage 4, unmapped 171) · conflicts 0 · schema delta
0 · main внутри 25: **0/25/0** · orphan 0 · attrs_cache 25/25 ·
**sandbox vs staging: совпадение по всем 9 структурным пунктам** —
точных отличий staging от sandbox-expectation нет.

## Recovery artifacts

`03-recovery-plan.md` (host `/home/taximeter/proff58-staging/var/vi02/` +
локально `scratchpad/vi-int-02/`): backup path+sha256, B2, created-сеты
(26 пар PAV, 69 image ids), готовые rollback-команды
(`catalog_images_ops --source vseinstrumenti --since 2026-09-05T08:06:00Z`
+ PAV по provenance), критерий возврата к B2. Журнальные ImportRun 85/86
не откатывать. Rollback — только по отдельному OWNER GO.

## Оставшиеся moderation/unmapped

dropped_select 11 (значения вне словаря опций), skipped_voltage 4 (сетевые
товары — по контракту), unmapped 171 имя (~205 значений) — вход для
отдельного mapping/ROI-аудита; массовое создание атрибутов запрещено.

## Новые GAP

- **G-1 (инфра, закрыт чужим треком):** build-context 940 МБ на
  VPS-checkout → deploy-таймауты. PR #684 (.dockerignore) устранил корень.
- **G-2 (наблюдение):** deploy.yml `command_timeout: 30m` остаётся узким
  местом при перегруженном хосте даже с .dockerignore; статус run'а может
  быть красным при фактически завершившемся deploy — сверять состояние на
  хосте, а не только статус в Actions.
- Новых GAP по данным/коду VI-трека нет.

## Запреты после pilot (в силе)

long-tail 128 — NO · массовое создание Attribute/Option — NO · применение
unmapped/moderation — NO · unit conversion — NO · второй importer — NO ·
legacy image cleanup (14 товаров без main) — NO · расширение pilot за
пределы 25 fixture — NO.

## Артефакты

`proff58/scratchpad/vi-int-02/` (+ копии `/home/taximeter/vi02/` и
`/home/taximeter/proff58-staging/var/vi02/`): 03-baseline-B2.json,
03-dryrun-1.json, 03-chars-apply(-2).json, 03-images-apply-1/2.json,
03-postaudit-images.json, 03-postaudit-final.json, 03-recovery-plan.md,
deploy-manual.log, pytest-junit всех прогонов, staging-main-audit.json.
