# VI-INT-13 — Wave 2A Image Apply (6 strict / 16 refs) → **PASS**

Дата: 2026-09-10. Продолжение `2026-09-10-vi-int-12.md` (COLLECTION_COMPLETE).
IMAGE-ONLY трек: характеристики не применялись (deployable CURRENT yield = 0,
все 6 типов вне deployed scope); scope ради 15 raw-map PAV не расширялся.
Данные остаются на staging.

## Вердикт: PASS

- provenance complete · existing image mutations 0 · existing main
  mutations 0 · duplicate (product,source_url) 0 · duplicate
  (product,checksum) 0 · introduced main>1 = 0 · orphan files 0 ·
  **PAV created 0 / updated 0 / attrs_cache unchanged (image-only
  инвариант)** · schema/taxonomy zero-write · foreign mutations в scope 0 ·
  rerun = полный no-op (downloads 0).

## 1. Revalidation

6/6 clean (`13-revalidation.json`): все exists, active+stock, article ==
карточке, brand == карточке, tool_type == карточке, source card та же,
strict identity валиден (article_exact+brand_exact ×6), content_locked
False, PAV=1 (только tool_type), images 0, main 0, чужого активного
write-track нет, свежих `needs_review` нет.

## 2. Drift относительно VI-INT-12

**Отсутствует.**

## 3. Immutable manifest + SHA256

`13-image-manifest.json`, 6 items, deterministic ordering, пометка
`chars: NOT_APPLIED`, **SHA256
`02b4d980c00dc68e0138e14759b01718d080fde8c47d05fb52b244cf882e45c1`**
(runner проверял при каждом write — `MANIFEST_SHA256_OK`).

## 4. CURRENT characteristics verification = zero deployable

Подтверждено read-only: deployable CURRENT yield = **0** (все 6 типов вне
deployed scope 40); importer в write mode не запускался. Raw-map 15 PAV —
сохранён как evidence, не применён (раздел 29).

## 5. Image preflight

refs **16** (min 1 / median 3 / max 3) · existing images 0 · existing
vseinstrumenti 0 · existing main 0 · source_url collisions 0 · input
дублей 0 · invalid (M-13) 0 · expected downloads 16 == факту.

## 6. G-4 global baseline

`13-baseline-global.json`: Attribute 108, Option 671, PAV total 78283,
ProductImage 771, migrations 127 (+source dist; staging растёт чужими
треками — baseline-факт, не наш delta).

## 7. G-4 scope baseline

`13-baseline-scope.json`: по 6 — exact PAV set (поля + source),
attrs_cache, exact ProductImage set (= пусто).

## 8. Backup + SHA256 + validation

`db-20260910-064203-pre-vi-int13.dump` (22 711 688 б), sha256
`4519cf9f…e88f`, exit 0, TOC 733 / TABLE DATA 72, persistent, df ≥2 ГБ.
Снят после freeze, непосредственно перед write.

## 9. Recovery plan

`13-recovery-plan.md`: rollback ТОЛЬКО ProductImage с provenance VI-INT-13
(product ∈ manifest + source=vseinstrumenti + отсутствовали в baseline +
source_url/checksum совпадают) + обязательное foreign records = 0. Без ID
ranges, без «всё после timestamp», без global source deletion.

## 10. Pre-write drift gate

**CLEAN** — 6/6 без изменений против frozen manifest.

## 11. Pilot

24740 (deterministic first, наименьший id): created 3 (ids 854–856),
provenance полный, файлы на месте, галерея была пуста → ровно 1 main
(854), existing mutations 0, duplicates 0, PAV не тронут (count + source
== baseline). **Pilot PASS.**

## 12. Full image apply

16 refs → **created 13 + dedup 3 пилота = 16/16**, rejected 0.

## 13. Refs/downloads/created/dedup/rejected

refs 16 · downloads 13 (пилот 3 до сети уже был) · created 13+3 ·
dedup 3 · rejected 0. Искусственных «16 creates» не требовалось — dedup
по контракту.

## 14. Provenance audit

У всех новых записей: source=vseinstrumenti, source_url заполнен,
checksum 64 hex == sha256 файла, fetched_at в окне прогона, product ∈
manifest. **Complete.**

## 15. Main invariant

main>1 = 0 · существующий main не демотирован · **6 товаров → ровно по
одному main (первое созданное)** · rerun main не меняет.

## 16. Duplicate audit

duplicate (product_id, source_url) = 0 · duplicate (product_id, checksum)
= 0 · duplicate main = 0.

## 17. Physical media/orphan audit

missing files = 0 · orphan files, созданные VI-INT-13 = 0 · ordering
(-is_main, sort_order, pk) подтверждён.

## 18. G-4 attribution

**global ProductImage delta +16 == VI-INT-13 exact created set +16** (16
id по provenance) · foreign delta в окне = 0 · foreign vseinstrumenti вне
6 в окне = 0 · foreign mutations внутри 6 = 0.

## 19. Foreign staging activity

0 в окне write. Staging в целом живой (baseline п.6), но конкурентных
записей в наше окно не было.

## 20. PAV zero-write audit

Scope PAV по 6 **побайтово before==after** · created 0 · updated 0 ·
attrs_cache unchanged. **Image-only инвариант соблюдён.**

## 21. Schema/taxonomy zero-write audit

Attribute 108=108 · Option 671=671 · CategoryAttribute 255=255 ·
migrations 127=127 · taxonomy/tool_type данных не изменилась.

## 22. Mandatory rerun — полный no-op

created 0 · **downloads 0** (все 16 refs dedup до сети, окно 0,06 с) ·
existing rows unchanged · main flags unchanged · checksum/provenance
unchanged.

## 23. Final integrity audit — ALL_OK

manifest SHA сверялся при каждом запуске · exact image/PAV before-after ·
provenance/checksums полны · dup URL/checksum 0 · main distribution
**0/6/0** · orphans 0 · schema/taxonomy counts неизменны · foreign
mutations 0 · 8 контейнеров healthy · source_dist: vseinstrumenti
**212** (196+16), прочие без изменений.

## 24. Actual image yield

**16 изображений** (6 товаров).

## 25. Products meaningfully enriched

**6** (image-only, как и планировал image-first трек).

## 26. Actual long-tail recount

По predicate (не «70−6»): **remaining 70 → 64** — выбыли ровно наши 6
(появились изображения); foreign exits в окне = 0; без изображений
остаются 64.

## 27. Remaining strict/source-card state

Со strict cards: только **22673** (без refs). Excluded: no_source_card
57 · out_of_scope 51 · skm_or_conflict 11 · UNRESOLVABLE 1 · 3
SCOPE_DRIFT-товара (needs_review июля) — вне scope.

## 28. Shtoborezy failure ledger — unchanged

4 ЗШ-П промаха — доказанный source-coverage GAP; retry/matcher/query
variants/model-only не применялись.

## 29. Raw-map 15 PAV — NOT APPLIED

Evidence сохранён (raw-map 15 / deployable 0); решение о дешёвом
расширении existing scope без schema v2 — отдельное, после этого трека.

## 30. Old strict cards — NOT APPLIED

31935, 31938, 26863, 28319, 33252, 42549, 22673 — не применялись;
объединение с 43824 gravery в cleanup/research track — отдельное решение.

## 31. Schema v2 = NOT STARTED

## 32. Wave 2B/2C = NOT STARTED

## 33. Новые GAP/incidents

Нет. Process lesson из VI-INT-12 сохранён: checkpoint должен быть
исполняемым механизмом (пауза после 5 outcomes физически не сработала по
таймингу; collector code в этом треке не менялся — отдельный
TECH_DEBT/operations вопрос).

## 34. Recommendation следующего шага

- **Strict inventory снова исчерпан** (22673 без refs — недоступен
  источником). Следующие волны — только новый сбор: Wave 2B (gravery) или
  пересмотр Wave 2C слоя (часть I3-типов может дать image-first yield —
  shtoborezy-класс исключён ledger'ом).
- **Документация:** VI-INT-11/12/13 отчёты лежат незакоммиченными в
  worktree — кандидат на следующий docs closeout PR (по образцу VI-INT-10).
- **Raw-map 15 PAV + old strict (6 карточек) + gravery 43824** —
  возможный небольшой cleanup/research track, если будет OWNER GO.
Решение — за владельцем.

## Артефакты

20 файлов `13-*`: `/home/taximeter/proff58-staging/var/vi02/` + локально
`proff58/scratchpad/vi-int-02/` (revalidation, manifest+sha256, preflight,
baselines, recovery-plan, drift-gate, apply/postaudit, attribution, rerun,
postaudit-final, longtail-recount).
