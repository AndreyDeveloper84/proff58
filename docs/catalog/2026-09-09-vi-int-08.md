# VI-INT-08: RESEARCH_COMPLETE

Targeted Collector Research — финальный отчёт. Всё read-only: staging writes — нет,
map/schema — не менялись, collector массовый — нет (только контрактный targeted run 16),
git — нет. Артефакты: `/home/taximeter/vi02/08-*` + `scratchpad/vi-int-02/08-*`.

## 1. Цель трека

Дособрать доказательную базу для решения о map schema v2 / следующем batch'е long-tail:
8 tool_type'ов с тонкой evidence (VI-INT-05), 16 товаров targeted pool.

## 2. Current remaining scope

- Long-tail remaining после Batch B: **86** (101 − 15).
- С картами на диске: **13 новых strict** (research-16) + 6 старых unused strict
  (типы отклонены simulation как 0-safe-PAV; boltorezy тогда оценивался без «Длина»-анализа).
- Без карточек: **~73**.

## 3. Collection track (research-16)

16 товаров (revalidation 16/16 clean): run-1 — 10 MATCHED + 1 SKM, остановка
SESSION_BUDGET_STUCK на 42072; досбор rest-файлом (4 товара) — 3 MATCHED + 1 SKM.
**captcha 0 за весь трек**, контракт не ослаблялся. Итог: **13 MATCHED_STRICT /
2 SEARCH_KEY_MISS / 1 UNRESOLVABLE_IN_BUDGET**.

## 4. Identity results

13 strict (у каждого reason=article_exact+brand_exact — проверено): chars 162,
image_refs 37, все без текущих фото. NOT_FOUND: 7335 ('71/4/32' — топливный фильтр
вместо воздушного), 44181 ('ЗМ-35-1600 ВК' vs 'ЭК'). UNRESOLVABLE_IN_BUDGET: 42072
(общий числовой артикул '20805', ~12–16 loads > budget 9 — исключён из sample).
`08-identity.json`.

## 5. Existing-map replay (current deployed ruleset)

По 13 новым strict production-кодом на staging (read-only): **8/13 enrichable
(без scope-фильтра), 11 safe PAV** (weight_kg 8, power 3). С учётом deployed scope=40:
**covered 5 (22473, 42071, 44144, 44145, 44177), enrichable 4, PAV 7**,
dropped_select 0, conflicts 0. `08-existing-map-replay.json`.

## 6. «Тип» semantic matrix (79 карт)

Однозначных per-tool_type семантик ровно **2**: dinamometricheskie = механизм
('щелчковый' ×3), nabory-klyuchey = тип шлица (Torx ×4). У новых типов
(miksery/otboynye/boltorezy/machete/izm-dalnomery) поля «Тип» в карточках НЕТ.
Остальное — дубли названия/tool_type (DROP) или назначение (AMBIGUOUS).
Generic type — никогда. `08-type-matrix.json/.md`.

## 7. «Длина» / units matrix (79 карт)

safe mm → length: boltorezy ×4 (350/300/450/600, названия подтверждают),
shtifty ×1 ('6х41 мм'), trubogiby ×1 (460); probable medium: obor-shlangi 290,
bp-generatory 705. roll-m → tape_length (без конверсии): lenty-binty ×5,
fiksatory ×2 ('(55 м)'/'110 м' в названиях!), krep-provoloka ×2 ('200м'/'100м').
Unit-trap: 0. Вывод: одно имя — две цели, только per-tool_type (schema v2),
global DIRECT запрещён. `08-length-matrix.json/.md`.

## 8. Pack B candidates (79 карт)

`08-packb-candidates.json/.md`: Класс товара (15p, NEW attr select ×3),
Цвет (14p, NEW attr), Диэлектрическое покрытие (12p — INSUFFICIENT, все 'нет'),
Назначение (8p, AMBIGUOUS), Трещотка (7p, NEW attr boolean), Тип патрона
(otboynye sds-max→chuck SAFE; М14/М27 миксеров — REJECT), Min усилие (4p, NEW attr),
Объем (4p, unit-chaos).

## 9. Targeted collector research (исполнен)

16 товаров, ~60 loads, 6 сессий, 2 захода + 1 досбор. Контракт соблюдён
(probe, budget 9, cooldown 9000, captcha → стоп без обхода).

## 10. Scenario A — CURRENT MAP

По remaining 86 с 13 картами: **covered 5, enrichable 4, PAV 7, image-capable 5
(refs 15)**. Плюс факт Batch B (15/15, 23 PAV, 38 images). Хвост на старых картах:
120 dropped + 170 unmapped (предмет Pack B).

## 11. Scenario B — MAP SCHEMA V2 + EXISTING ATTRIBUTES

Маппинги (per-tool_type): boltorezy «Длина»→length (4 товара, +4 PAV, weight +2),
siz-nakolenniki scope (+2 weight), «Тип патрона»→chuck per otboynye (+1),
«Тип»→bit_type per nabory-klyuchey (0 remaining, dobor 3). Исключены:
«Тип»→механизм (нет existing attr → Scenario C), «Длина»→tape_length (все applied,
dobor ~9), izm-dalnomery (54/54 unmapped), bp-podgotovka (0 на 2 картах).
**Yield: ~7 товаров / ~9 PAV + ~12 PAV dobor на applied.**

## 12. Scenario C — V2 + NEW ATTRIBUTE CANDIDATES (simulation)

+Класс товара (15p), Цвет (14p), Трещотка (7p), Min усилие (4p), механизм (3p) →
**+43 потенциальных PAV** (большая часть — на applied товарах). Только simulation,
создание schema не разрешается.

## 13. Image-first

13/13 strict с refs (37), все без текущих фото. Image yield: **13 товаров/37
изображений**, все 13 выбывают из long-tail («без фото»). Стратегии:
characteristics-first — тонко (4/7); image-first — каждый товар получает контент;
**combined — 13 images + 7 PAV, максимум на товар при нулевом расширении ruleset**.
`08-image-first.json`.

## 14. Pack A — APPLY WITH CURRENT MAP (не применять)

Кандидатский вход следующего controlled batch: **42071, 44144, 44145, 44177**
(enrichable, 7 PAV) + 22473 (image-only in-scope); out-of-scope strict 8;
refs 37. Отдельный OWNER GO.

## 15. Pack B — JUSTIFIES MAP SCHEMA V2

**ТОНКИЙ: 3–4 mapping'а, ~7 товаров, ~9 PAV remaining + ~12 dobor.** Состав с
evidence и negative fixtures — `08-decision-packs.json/.md`.

## 16. Pack C — SCHEMA/RESEARCH LATER

NEW attributes (Класс товара/Цвет/Трещотка/Min усилие/механизм); unit research
(Объем); AMBIGUOUS (Назначение, Диэлектрическое покрытие); REJECT (М14/М27 миксеров);
42072-класс (collector contract); izm-dalnomery — 54 unmapped, отдельный research осей.

## 17. Simulation vs факт

Simulation VI-INT-05/06 (Pack A) подтвердилась фактом Batch B 1:1 (23 PAV план =
23 факт, 15/15). Replay research-карт — production-кодом на staging.

## 18. Recommendation (по цифрам)

**R4 / image-first combined batch**: следующий write-track — combined apply для 13
research-strict: images 37 refs (все 13) + characteristics по current map для
5 in-scope (7 PAV). Обоснование: image yield (13/37) на порядок больше chars yield
(4/7) при нулевом расширении ruleset; 13 товаров выбывают из long-tail.
Schema v2 — DEFER (тонкий gain ~9 PAV против стоимости формата+кода+regression;
evidence уже упакован в Pack B — реализация станет механической, когда ROI созреет).
NEW attributes — research later (Scenario C — simulation).
Массовый сбор 73 uncarded — NO (доказанная непокрытость типов и
UNRESOLVABLE-класс; точечные пробники по решению владельца).

## 19. G-4 протокол атрибуции (для следующего write-track)

Staging живой: чужие треки пишут (доказано в Batch B: +414 keyword-PAV во время
нашего окна). Обязательства следующего track: baseline непосредственно перед write,
per-product provenance diff (created-сет), атрибуция по source= (scraper vs keyword),
журнальные ImportRun не в счёт данных.

## 20. load_attributes drift

Untouched: scoped load VI-INT-06 (+2 options) — единственная запись трека options;
pre-existing drift (battery_capacity + 8 bindings) не применялся и не применяется.

## 21. Риски

Unit-trap «Длина» — закрыта классификацией (никакого global); «Тип патрона» миксеров —
REJECT зафиксирован; 42072-класс — исключён; captcha-эскалации — нулевые;
unmapped-хвост 170+54 — предмет Pack B/research, не молчаливой записи.

## 22. Артефакты

`/home/taximeter/vi02/08-*` (16 файлов) + локально: 08-research-manifest(+sha256),
08-research-pool(-rest).json, 08-revalidation, 08-identity, 08-existing-map-replay,
08-type-matrix(.md), 08-length-matrix(.md), 08-packb-candidates(.md),
08-scenarios(.md), 08-schema-v2-roi.md, 08-image-first, 08-decision-packs(.md),
08-research-scope-status, 08-report.md.

## 23. Отклонения и честные оговорки

- «8/13 enrichable» части 1 — без scope-фильтра; с deployed scope=40 — 4/7 PAV
  (зафиксировано в сценарии A).
- 6 старых unused strict-карт — их типы отклонялись без «Длина»-анализа;
  boltorezy реабилитирован матрицей (safe mm ×4) — вошёл в Pack B.
- 42072 — UNRESOLVABLE_IN_BUDGET: исключён, не добивался.

## 24. Точный proposed scope следующего track

1. **Combined controlled batch (GO владельца)**: 13 research-strict —
   images apply 37 refs по существующему pipeline + chars apply по current map
   (7 PAV: power ×3, weight_kg ×4) для 5 in-scope; протокол VI-INT-07
   (backup/baseline/drift-gate/provenance/rerun).
2. **Deferred**: map schema v2 (Pack B состав готов) — когда ROI созреет
   (после combined batch + накопления per-tool_type evidence).
3. **Research later**: NEW attributes (Scenario C), izm-dalnomery-оси,
   точечные пробники типов без карт — только по отдельному GO.

## 25. Вердикт

**VI-INT-08: RESEARCH_COMPLETE.** 16/16 пула обработано контрактно (13 strict +
2 SKM + 1 unresolvable), evidence для всех решений собрана и упакована,
рекомендация R4 (image-first combined) вытекает из цифр. Ничего не применялось,
Batch C не запускался.

После отчёта — стоп.
