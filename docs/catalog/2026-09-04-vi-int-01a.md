# VI-INT-01a — закрытие блокеров PARTIAL → gate пройден

Дата: 2026-09-04. Продолжение `2026-09-04-vi-int-01.md`.
Итог: **все gate-условия владельца выполнены**, следующий этап —
VI-INT-02 sandbox (отдельный GO на выполнение уже дан; staging apply
по-прежнему запрещён).

## Исправления

### GAP-1 — brand aliases (sync, один canonical source)

- Canonical source алиасов — proff58 `data/catalog_processing_rules/
  brand_vocabulary.json`; wb_p держит вендорную копию (не наоборот).
- Правка: к REMOCOLOR добавлен `ремоколор`, к PATRIOT — `патриот`,
  добавлен бренд REDIUS [`redius`, `редиус`] — побайтово по эталону
  proff58-трека кириллических алиасов (worktree `.worktrees/cyrbrand`,
  тот же набор, что в вендорной копии wb_p; никаких special-case под
  товар 15728).
- Regression test: `apps/catalog/test_brand_vocabulary_cyrillic.py`
  (18 тестов, включая список сознательно ОТВЕРГНУТЫХ форм с причинами —
  защита от «дополнения догадками»). Зелёные.
- Контейнер для повторного dry-run: файл данных обновлён в
  proff58_staging-web-1 (эфемерная fs контейнера, backup сохранён;
  не БД; постоянный путь — деплой этой же data-правки).

### GAP-2 — unit trap (консервативно)

- «Длина» и «Мощность двигателя» выведены из DIRECT в `unmapped`
  (map v2: 13 mapped / 171 unmapped, покрытие 41/250). Причины записаны
  в самой карте. Никакой unit-aware конвертации и угадываний.
- Гейт единиц ДРФ-1440 офлайн пройден.

### GAP-4 — ImageSource

- `ImageSource.VSEINSTRUMENTI = "vseinstrumenti"` (14 симв., max_length=16)
  + миграция `0038_image_source_vseinstrumenti` (alter choices, без
  DB-эффекта). Новый pipeline не создавался.
- Проверки аддитивности: `process_batch` валидирует `source ∈
  ImageSource.values` — расширение не меняет поведение существующих
  источников; provenance/idempotency/checksum/rollback покрыты
  существующими тестами `test_image_pipeline.py` /
  `test_image_reversibility.py` — 20 DB-free тестов зелёные; 26
  DB-backed падают ТОЛЬКО из-за локальной аутентификации postgres
  (pre-existing ограничение среды, к изменению отношения не имеет).
- `ImagePipeline.process_batch` подтверждён кодом как подходящий:
  аддитивность, `is_main` только при пустой галерее, дедуп по
  (product, source_url) и (product, checksum), content_locked,
  manual в batch запрещён.

## Повторный DRY-RUN (staging, те же 25 fixture)

Два независимых прогона `catalog_import_scraped --category vi-longtail
--dry-run`:

```
прогон 1: matched 25 · ambiguous 0 · not_found 0 · create 26 ·
          confirm 0 · conflict 0 · skipped_voltage 4 · dropped 11
прогон 2: идентично агрегатам; отчёты ПОБАЙТОВО одинаковые
DB baseline до/после: ИДЕНТИЧЕН (sha256 совпал, DB_UNCHANGED)
```

## Gate

```
products 25/25 ✓   identity 25/25 ✓ (все SKU-ступенью, 15728 —
                   article confirmed)   ambiguous 0 ✓
false assignments 0 ✓   unit-trap 0 ✓ (в плане нет length/power_hp)
automatic schema expansion 0 ✓ (171 unmapped → report; 11 select →
dropped; опции/атрибуты не создаются)
```

## Новая разбивка

```
safe_import (create)      26   (weight_kg 15, width 2, diameter 2,
                                disc_diameter 2, height 1, bore 1,
                                material 1, voltage 1, power 1)
dropped_select            11   (moderation; опции не создаются)
skipped_voltage            4   (сетевые товары, по контракту)
unmapped_moderation      171 имён / ~205 значений — вход для отдельного
                               mapping/ROI-аудита, НЕ для массового
                               создания атрибутов
```

## Коммиты (локально, без push/PR)

1. VI-INT-01: adapter + fixture + 9 контрактных тестов + evidence-отчёт.
2. VI-INT-01a: brand vocabulary sync + cyrillic regression test + map v2 +
   ImageSource + миграция 0038 + этот отчёт.

## Следующий этап

VI-INT-02 sandbox (GO владельца получен): 25 fixture → identity →
characteristics → images → sandbox DB → post-audit → rerun/no-op →
rollback; проверка отсутствия UPDATE приоритетных значений; images с
`source=vseinstrumenti`, source_url/checksum/порядок/лимит/повтор no-op/
rollback только своих. Staging apply, long-tail 128, массовое создание
Attribute/AttributeOption, второй importer, unit-conversion архитектура —
запрещены до результатов VI-INT-02.
