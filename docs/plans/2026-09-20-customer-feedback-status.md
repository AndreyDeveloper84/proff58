# Обратная связь от 20.09.2026: состояние задач и приёмка

Ветка `feature/customer-feedback-0920` от `origin/dev`, 12 коммитов, не отправлена.
Задачи в Linear не заведены: интеграция в сессии не была авторизована. Описания задач —
`2026-09-20-customer-feedback-tasks.md`, решения по реализации и итоги ревью плана —
`2026-09-20-customer-feedback-implementation.md`. Отметки ниже готовы к переносу в тикеты.

Снятый флажок означает «не проверено» или «ждёт выкладки», причина указана рядом.

## Проверки ветки

| Проверка | Результат |
|---|---|
| Тесты витрины | 76 файлов, 512 тестов, все зелёные (было 72 и 440) |
| Типы витрины | ошибок нет |
| Линтер витрины | ошибок нет; одно предупреждение было и до правок |
| Сборка `next build` | проходит |
| Тесты бэкенда: каталог, заказы, AI, сквозные | 4146 прошли, 1 упал |
| Упавший тест | `test_two_parallel_decisions_at_most_one_applied` — параллельная обработка каталога, эти файлы не менялись; отдельным запуском проходит (8 из 8) |
| Сквозной путь в браузере | главная → бренд → категория → сортировка → быстрый просмотр → покупка → закрытие → корзина → количество → оформление; ошибок JS — 0 |

## UX-06. Ручной ввод количества в корзине — P1 — `6f44750`

- [x] Можно заменить 1 на 15 или 159; строка, итог и счётчик берутся из ответа сервера.
- [x] Значение сохраняется после перезагрузки страницы.
- [x] Пустое поле допустимо при наборе и не отправляется. Единое правило: возвращается
      прежнее число и показывается «Введите целое число от 1».
- [x] Ноль, минус, дробь, текст и небезопасное целое не отправляются; ноль товар не удаляет.
- [x] Лимита 99 нет. Остаток на этапе корзины backend не проверяет, фронт его не выдумывает.
- [x] При отказе сервера введённое не выдаётся за сохранённое: «Не сохранено», причина — в
      общем сообщении страницы.
- [x] Enter и следующий за ним blur дают один запрос; «ввёл число и нажал +» не откатывает
      введённое; двойной клик по «+» — одно изменение.
- [x] Поле вмещает многозначное число, подписано для скринридера, на время запроса
      `readOnly`, а не `disabled` — фокус не теряется.

Пробел контракта закрыт: количество больше предела столбца отвечало 500, теперь 400;
повторное добавление к огромной строке не переполняет столбец.

## UX-07. Страница бренда вместо поиска — P1 — `7ac91dd`

- [x] Metabo и каждый бренд блока открывают `/brands/<slug>`, а не поиск.
- [x] В выдаче только товары с этим брендом в поле бренда, включая товары без бренда в
      названии; чужой товар со словом в названии или описании не попадает.
- [x] Сортировка, категории, пагинация и прямое открытие URL сохраняют бренд; количество
      равно полной выдаче, а не длине страницы.
- [x] Регистр и написания сопоставляются существующим механизмом транслитерации;
      «Ресанта» даёт `resanta`.
- [x] Неизвестный бренд — настоящий 404; известный без товаров — страница «Товаров
      <Бренд> сейчас нет» со ссылкой в поиск.
- [x] Стабильность ссылок решена: страница бренда использует slug без суффиксов `-2/-3`,
      все написания с одним slug сливаются. Решение описано в `brand_slugs.py`.
- [x] Тест плиток обновлён; добавлено 14 тестов API бренда и тесты адаптера.
- [ ] Проверка на боевых данных — после выкладки бэкенда.

Важно: поле «бренд» заполнено у малой доли товаров. Точная выдача Metabo — 2 товара,
поиск находит 57; у DeWALT, AEG, Milwaukee, Hilti и Stanley точная выдача пуста. Под
выдачей стоит подсказка «ещё N товаров упоминают бренд в названии» со ссылкой в поиск.
Заполнение поля — готовая команда `catalog_fill_brand`, запуск с записью решает владелец.

## PERF-01. «Бренды не нажимаются» — P1 — `046c1d8`

- [x] Условия и запись воспроизведения приложены:
      `docs/perf/2026-09-20-brands-click-investigation.md` и сырые данные рядом.
- [x] Причина установлена, есть замеры до и после. До: от клика до любого отклика
      2,1–10,4 с. После: 13–106 мс.
- [x] 20 последовательных переходов (холодные и тёплые, desktop и mobile): потерянных
      нажатий нет, ожидания без индикации нет.
- [x] Зависший API даёт экран ошибки с «Повторить» через 8,1–8,4 с, ошибочный — через 0,1 с.
- [x] Расследование не закрыто добавлением спиннера: убран медленный поиск с пути плиток,
      запросы идут параллельно, добавлены таймауты.
- [x] Сценарии повторены на маршрутах брендов после UX-07.
- [ ] Задержка «в несколько минут» не воспроизведена: устройство, сеть и время события
      неизвестны. Вероятная причина — холодный старт после выкладки; нужна сверка с
      логами сервера.
- [ ] Бюджет «выдача до 3 секунд» на боевом стенде — после выкладки.

## UX-01. Светлая тема по умолчанию — P2 — `e856aa0`

- [x] В новом профиле сайт светлый при тёмной теме ОС; смена темы ОС на лету сайт не меняет.
- [x] Сохранённая тёмная тема переживает перезагрузку и переходы.
- [x] При недоступном или повреждённом localStorage сайт светлый и работает.
- [x] Вспышки противоположной темы нет, иконка переключателя соответствует теме.
- [ ] Проверка на физическом мобильном устройстве не проводилась; проверено в эмуляции.

## UX-02. Карточки отделены от фона — P2 — `b933a2e`, `54b4f50`

- [x] Карточка отделяется от фона без наведения в обеих темах.
- [x] Оформление едино у вариантов главной, сетки и списка; наведение и фокус различимы.
- [x] Цена, название, наличие и кнопки читаются; шапка, подвал и поля форм не затронуты.
- [x] Скриншоты главной и каталога до и после, 375 и 1440 px, обе темы.

## UX-04. Шрифт и элементы крупнее — P1 — `b933a2e`, `feb7d8f`

- [x] Основной контент читается без увеличения; исключение из шкалы одно и обосновано.
- [x] Названия, цены, характеристики и кнопки не перекрываются и не обрезаются.
- [x] На 320, 375, 768 и 1440 px горизонтальной прокрутки нет; то же на 720 px, что
      соответствует масштабу 200 %.
- [x] Диалоги помещаются в экран, прокручиваются внутри, закрытие закреплено.
- [x] Таблица итоговых размеров — `docs/design/2026-09-20-type-scale.md`, скриншоты рядом.
- [ ] Статьи и личный кабинет в постановку не входили, там остались подписи 10–11 px.

## UX-03. Копирование телефона и адреса — P2 — `ab8b319`

- [x] Копируется отображаемый номер без `tel:` и полный адрес, даже под короткой подписью.
- [x] Работает мышью, касанием и с клавиатуры; фокус видим; уведомление через `aria-live`.
- [x] Уведомление только после успешного копирования; повторные нажатия очередь не создают.
- [x] При запрете или отсутствии Clipboard API — понятное сообщение и выделенный текст.
- [x] Шапка, подсказка «Контакты», мобильное меню, подвал, страница 404; «Позвонить»
      открывает `tel:`.
- [ ] Ручная проверка буфера на HTTPS и в мобильном браузере — после выкладки.

## DATA-01. Характеристики товаров — P1 — `a39899b`

- [x] По характеристике понятно, что означает число; строк из безымянных значений нет.
- [x] Единицы не повторяются и не теряются; ноль и «Нет» сохраняются; пустые параметры
      строк не создают.
- [x] Порядок по типу товара, а не по алфавиту; один и тот же у карточки, быстрого
      просмотра и страницы товара.
- [x] Длинные значения переносятся, таблица читается на 320–375 px.
- [x] Товар без характеристик имеет аккуратное пустое состояние, значения не выдумываются.
- [x] Проверена выборка из 20 реальных товаров 15 разделов и срез 947 товаров; примеры
      API и правила исправления — `docs/catalog/2026-09-20-specs-audit.md`.
- [x] Список проблем данных с конкретными товарами приложен. Представление готово
      отдельно от наполнения.
- [ ] Наполнение: у 45 % товаров среза нет характеристик, у 42 % пуст бренд — работа
      контента и правил извлечения.

## UX-05. Быстрый просмотр — P2 — `98c6cba`

- [x] Работает из главной, каталога, поиска и страницы бренда; после закрытия фильтры,
      сортировка и позиция прокрутки на месте.
- [x] Фото переключаются; товар без фото, цены и характеристик имеет корректные состояния.
- [x] Покупка идёт через общий CartProvider, счётчик обновляется; покупка, избранное и
      сравнение окно не открывают.
- [x] Загрузка, ошибка с повтором, защита от ответа предыдущего товара.
- [x] Закрытие кнопкой, Escape и кликом по фону; фокус заперт в окне и возвращается на карточку.
- [x] Ctrl/Cmd-клик открывает страницу товара; «Подробнее» ведёт на правильный товар.

## Что нужно от людей

1. Подключить Linear и завести девять задач; отметки приёмки взять отсюда.
2. Решить, отправлять ли ветку и открывать ли PR в `dev`.
3. Решить судьбу `catalog_fill_brand --apply` на стенде: без него страницы брендов бедны.
4. Учесть пересечение с незакоммиченными правками основного дерева: шапка, подвал,
   корзина, layout, плитки брендов, кнопка покупки. Конфликты решает тот, кто вольёт вторым.
