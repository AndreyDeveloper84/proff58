import { redirect } from "next/navigation";

// Избранное переехало из кабинета на витрину (/wishlist): сохранять товары можно
// и без аккаунта. Редирект оставлен ради сохранённых ссылок и закладок.
//
// Страница намеренно ВНЕ группы (guarded) и исключена из proxy.ts: и гвард
// кабинета, и proxy разворачивают гостя на форму входа раньше, чем сюда дойдёт
// очередь, — и человек с закладкой упирался в «войдите» вместо своего избранного,
// которое входа больше не требует.
export default function AccountWishlistRedirect() {
  redirect("/wishlist");
}
